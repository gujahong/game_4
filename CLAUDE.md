# 이 프로젝트에서 지켜야 할 규칙

## 작업을 시작하기 전에

**먼저 `작업일지.md`를 읽을 것.** 컴퓨터 2대를 오가며 개발 중이라 대화 기록이 넘어오지 않는다.
"지금 어디까지 왔고, 최근에 뭘 왜 그렇게 정했고, 다음에 뭘 할지"가 거기 있다.

**세션이 끝날 때마다 `작업일지.md`를 갱신할 것.** 그게 다음 세션으로 맥락을 넘기는 유일한 수단이다.

## 사용자가 준 기본 규칙 (최우선)

1. 많은 부분을 수정해야 한다면 반드시 나에게 물어보고 진행해.
2. 하나의 파일에 코드를 다 넣지 말고, 기능별로 모듈화 해.
3. 요청이 명확하지 않을 때에 추론 및 실행하지 말고 우선, 내 설명을 제대로 이해했는지 말해.

## 화풍 — 회색 세상, 등불만 색

참고한 그림은 1비트 흑백 디더링 픽셀아트다. 그걸 이렇게 만든다.

```
PixelLab로 컬러 배경을 뽑는다  →  실행 중에 DitherFilter를 씌운다  →  등불만 필터 밖에 얹는다
```

**필터는 그림 파일을 바꾸지 않는다.** 셰이더가 화면에 얹히는 것뿐이라 디스크의 PNG는 컬러
원본 그대로다. 그래서 나중에 값 하나만 바꾸면 배경 전부가 한꺼번에 따라온다 — 배경이 서른
장이 돼도 다시 뽑을 필요가 없다. 필터를 씌워 구운 파일로 저장해두면 이 이점이 사라지니
하지 말 것.

### 고정된 값 (2026-08-11)

```
계조 4 / 디더 세기 1.0 / 탈색량 0.5 / 색 남길 채도 기준 0.3 / 경계 0.1 / 틴트 흰색
디더 격자는 화면에 고정 (dither_on_screen = true)
```

**격자를 그림이 아니라 화면에 못 박는다.** 그림에 붙여두면 확대할 때 격자도 같이 커지는데,
배율이 조금씩 변하는 연출(조우 화면)에서는 그때마다 격자가 다시 짜여 **그물이 표면 위를
기어다닌다.** 화면에 못 박으면 그림만 확대되고 격자는 멎어 있다. 컨셉과도 맞다 - 필터는
화면에 얹히는 것이지 그림에 붙어 있는 것이 아니다.

규칙은 한 줄이다 — **원본에서 쨍했던 것만 색이 남고, 나머지는 회색이 된다.** 예배당에서는
스테인드글라스와 촛불이 남았고, 숲에서는 젖은 이끼만 남았다. 숲의 나무가 회색인 것은 실패가
아니라 이 규칙의 결과다. 결과적으로 **색이 남은 곳이 곧 시선이 가는 곳**이 되므로, 배경을
주문할 때 "여기에 시선을 두고 싶다" 하는 것만 쨍하게 그리도록 쓴다.

주인공은 등불을 들고 다닌다. **등불은 배경 그림 안에 그려 넣으면 안 된다** — 배경은 통째로
필터를 통과하므로 같이 회색이 된다. 별도 노드로 필터 바깥에 얹어야 한다
(`FilterPreview._make_lamp()`가 임시로 그렇게 하고 있다).

값을 다시 맞추고 싶으면 `tools/FilterPreview.tscn`을 **직접 열어서** 실행한다(F5는 메인 씬을
띄우므로 이 도구가 아니다). 슬라이더로 돌려보고 C를 누르면 지금 값이 클립보드로 복사된다.
맞춘 값은 `shaders/DitherFilter.gdshader`의 uniform 기본값에 적어 넣는다 - 그게 유일한 원본이고,
`FilteredBackground`는 그 기본값을 그대로 쓴다.

## 그림 규칙: 화면에 놓일 크기 그대로 뽑는다

**모든 그림(UI 포함)은 게임 안에서 차지할 픽셀 크기와 똑같은 크기로 만든다.** 크게 뽑아서
줄여 넣거나, 작게 뽑아서 늘려 넣지 않는다.

게임 내부 해상도는 **960x540**이고 화면 전체를 정수배(1080p=x2, 4K=x4)로만 확대한다. 그래서
놓일 크기 그대로 만들면 도트 하나가 화면 픽셀 2개(또는 4개)에 딱 떨어져 절대 안 뭉개진다.
크기가 안 맞는 그림을 어쩔 수 없이 써야 하면 **확대/축소 대신 투명 여백을 자르거나 덧붙여서**
맞춘다.

### 사진은 512x512다 (2026-08-11)

**화면 전체를 덮는 배경은 이제 없다.** 960x540짜리 그림은 pixen의 면적 상한(262,144)을 넘어서
아예 못 뽑는다. 대신 장소 하나가 **512x512 정사각형 사진 한 장**이고, 화면에는 그 사진들이
겹쳐 놓인다(`PhotoStack`).

512x512는 **pixen이 뽑을 수 있는 가장 큰 정사각형**이다 - 면적 상한이 정확히 512x512다. 그리고
크기와 상관없이 전부 **1 generation**이므로, 더 작게 뽑는 것은 그냥 손해다. 320x320은
102,400픽셀이고 512x512는 262,144픽셀인데 값이 같다.

내부 해상도를 640x360에서 960x540으로 올린 이유가 이것이다. 512짜리 사진이 세로로 들어가야
하는데 360에는 안 들어간다. 1080p에 정확히 2배, 4K에 4배로 떨어지는 것도 960x540뿐이다
(1280x720은 1080p에서 1.5배라 도트가 깨진다).

## 그림의 축은 "압도감"이다 (2026-08-11)

회원님이 참고로 주신 그림이 열두 장인데, 소재도 색도 제각각이면서 **예외 없이 하나를
공유한다 — 거대한 것 하나와 티끌만 한 사람.** 그리고 회원님이 직접 못박으셨다:
**"색감은 크게 중요하지 않고, 코즈믹 호러·압도감·공포감이 중요하다."**

그러니 그림에 관한 판단은 전부 이 하나로 잰다. 이건 공포가 아니라 **경외**에 가깝다 —
괴물이 무서운 게 아니라 내가 하찮다는 걸 알게 되는 쪽이다.

배경을 주문할 때 지킬 것 넷.

1. **비교 대상을 반드시 넣는다.** 크다는 건 상대적이라, 크기를 아는 것이 없으면 아무리 크게
   그려도 안 커 보인다. **제일 중요하고 제일 자주 빠진다.** 단 **사람은 넣지 않는다**(회원님
   요청) — 사람이 없어야 "나 말고 아무도 없다"가 되어 고립감이 붙는다. 대신 **문·계단·묘비·
   철책·새 떼**를 쓴다. 그중 **문**이 제일 세다. 누구나 문 크기를 알기 때문이다
2. **화면 밖으로 잘려나가게 한다.** 전체가 프레임에 들어오면 "저만한 것"이 되어 작아진다
3. **정면 대칭.** "자연이 만든 게 아니다" 또는 "나를 기다리고 있었다"로 읽힌다
4. **다 보여주지 않는다.** 안 보이는 것이 무섭다 — 그래서 등불(얼마나 보이는가)이 곧
   공포의 손잡이가 된다

640x360이라 세부가 안 보이는 것은 약점이 아니라 이 방향에서는 이점이다. 상상이 채운다.

### 배경은 한 장으로 뽑지 않는다 — 초점은 따로 뽑아 얹는다 (2026-08-11)

**pixen에 640x360처럼 가로로 긴 캔버스를 주면 그림이 좌우로 갈라진다.** 한 장면을 넓게 그리는
대신 자기가 아는 정사각형 구도를 두 번 그려 붙인다. 처음 뽑은 넉 장(예배당·숲·탑 둘) **전부**
가운데 세로 이음매가 있다. 숲은 나무가 원래 여러 그루라 티가 안 났을 뿐 똑같이 갈렸다.

PixelLab 공식 답변이다(`agent_help`) — 추론이 아니다.

> Pixen은 정사각형에 가까운 구도에 최적화돼 있고, 가로로 긴 비율에서는 초점이 여러 개
> 생기거나 타일처럼 반복되는 현상이 자주 일어난다. 넓은 장면을 하나로 얻으려면
> `create_image_pro`(688x384)를 써라 — 타일링 없이 16:9를 다루는 유일한 모델이다.
> 다만 **예산이 빠듯하면 pro를 쓰지 말고**, 넓은 배경은 pixen으로 뽑고 주제는 정사각형
> + 투명 배경으로 따로 뽑아 엔진에서 합쳐라.

그래서 이렇게 한다.

| | 어떻게 | 왜 |
|---|---|---|
| 하늘·안개·벌판·숲·물 | pixen **640x360** | 초점이 없어서 갈라져도 티가 안 난다 |
| 탑·문·적·나무 한 그루 | pixen **정사각형에 가깝게 + `no_background=true`** | 하나여야 하는 것. 갈라지면 바로 보인다 |
| 합치기 | `FilteredSprite`로 배경 위에 얹는다 | |

**이러면 압도감도 같이 해결된다.** 주제를 따로 들고 있으면 화면에 얼마나 크게 놓을지 우리가
정한다 — 프롬프트로 아무리 부탁해도 안 되던 "화면 밖으로 잘리기"가 그냥 좌표 문제가 된다.

비용은 장소 하나에 **2 generations**(배경 1 + 주제 1)다. `create_image_pro`로 한 장에 담으면
20~40이라 10~20배 든다. 정사각형으로 뽑아서 잘라 쓰는 것은 **돈 주고 산 픽셀을 버리는 것**이라
공식적으로도 권하지 않는다.

### 화풍을 붙잡는 법 — 참조를 쓰지 마라 (2026-08-11)

그림마다 화풍이 흔들린다(성당은 붓질처럼 뭉근한데 숲과 탑은 검은 외곽선이 굵다). 원인은
`outline` 같은 파라미터를 안 정하고 뽑아서 **매번 모델이 알아서 골랐기** 때문이다.

**해법은 `create_image_pro`의 `style_image`가 아니다.** 우주쓰레기게임에서 이미 태워보고 남긴
기록이 있다(`Documents/연습/우주쓰레기게임/작업일지.md`, "규칙 2").

> 참조는 화풍만 가져오지 않는다 — **내용까지 복제한다.**
> 장면을 참조로 넣고 장면을 뽑으면 **그 장면이 거의 그대로 복제된다**(40회 x 2 낭비).
> 참조 이미지를 넣는 방법은 전부 실패했다(총 65회 이상 소모).

게다가 스타일 참조는 **캔버스가 작을수록 참조를 많이 넣고 화풍을 많이 배운다**(128→4장 /
256→1장). 우리는 448짜리 장면을 뽑으므로 **참조를 한 장밖에 못 넣는 구간**이고, 그게 바로
그쪽이 40회를 헛되이 태운 설정이다.

**통했던 방법은 이 넷뿐이다.** 전부 1 generation짜리다.

```
① 모델을 안 바꾼다        pixen만 쓴다
② 시드를 고정한다          한 번 정하면 안 바꾼다
③ 파라미터를 고정한다      view / direction / outline / detail
④ 어휘를 고정한다          같은 말투로 쓰고, 화풍을 끌고 가는 단어를 안 쓴다
```

④가 특히 중요하다. 그쪽 기록에 **"설명문에 절대 넣지 말 것"** 목록이 있다 — `sci-fi`,
`techwear`, `cybernetic`, `glowing panel`, `chibi` 같은 말이 화풍을 실사풍이나 서양 카툰으로
끌고 간다. 담백하게 쓴다.

### 물건으로 뽑지 말고 장면으로 뽑는다 (2026-08-11)

**투명 배경에 물건 하나만 뽑으면 아무리 크게 그려도 아기자기해진다.** 윤곽이 깔끔하고 공기가
없어서 "게임 에셋"으로 읽힌다. 실제로 탑을 그렇게 뽑아봤다가 회원님이 별로라고 하셨다
(`assets/props/tower.png`).

**좋았던 것은 첫 성당 내부**다. 그 차이는 이렇다.

| | 아기자기해진 것 | 좋았던 것 |
|---|---|---|
| | 투명 배경 + 물건 하나 | 배경 있는 **장면** |
| | 윤곽이 깔끔하다 | 안개·먼지로 윤곽이 풀린다 |
| | 화면에 여백이 많다 | **화면이 꽉 차 있다** |
| | 빛이 고르다 | 빛줄기가 들어와 명암이 갈린다 |
| | 깊이가 없다 | 안쪽으로 물러나는 공간이 있다 |

그래서 **위치 사진은 반드시 장면으로 뽑는다.** 실내이거나, 실외라도 안개와 지형으로 화면이
꽉 차 있어야 한다. 빈 하늘이 많으면 그것만으로 힘이 빠진다.

### 프롬프트로 "잘림"을 얻는 법 (실제로 두 번 뽑아보고 알아낸 것)

**생성기는 주제를 프레임에 다 담으려 한다.** "화면 밖으로 넘어가게 하라"고 말로 시키면 무시하고
꼭대기까지 얌전히 집어넣는다. 그러면 압도감이 통째로 사라진다. 지시가 아니라 **주어를 바꿔야**
한다.

| 안 되는 것 | 되는 것 |
|---|---|
| `a colossal tower rising past the top edge` | **`the foot of a colossal tower filling the frame`** |
| (하늘을 안 막으면 카메라가 물러난다) | **`almost no sky`** |
| `symmetrical` | 쓰지 말 것 — **좌우 거울 복제**가 되어 주제가 둘로 늘어난다 |

구름·새 같은 하늘 소재를 넣으면 모델이 하늘을 보여주려고 카메라를 뒤로 뺀다. 압도당하는
구도는 **하늘이 거의 안 보인다** — 눈앞이 전부 돌덩이다.

## 글꼴 (2026-08-11)

**Neo둥근모, 16px.** 1990년대 DOS 비트맵 글꼴 "둥근모꼴"의 트루타입 복각이고 SIL Open Font
License 1.1이라 게임에 넣어 파는 데 문제가 없다. `KoreanFont.gd`가 유일한 통로다 — 크기를
부르는 쪽에서 정하지 않고 `KoreanFont.NATIVE_SIZE`로 묶어뒀으니, 글꼴을 갈면 크기도 따라온다.

지켜야 할 것 셋.

- **네이티브 크기나 그 정수배로만 쓴다**(16, 32...). 어중간한 크기는 도트 격자가 어긋난다
- **기울임(`[i]`)을 쓰지 않는다.** 도트를 비스듬히 밀어버려서 격자가 통째로 깨진다
- 임포트 설정(안티앨리어싱/힌팅/서브픽셀 끄기)은 **글꼴 파일마다 `.ttf.import`에서** 꺼야 한다.
  project.godot의 `[gui]` 항목은 기본 글꼴에만 걸린다

갈무리(quiple)도 후보였고 파일에 남아 있다. **비트맵판(`*Bitmap.ttf`)은 쓰지 말 것** — 내장
비트맵이 특정 한 크기에만 들어 있어서 다른 크기에서는 글리프 높이가 0이 되고 글자가 통째로 안
그려진다. 외곽선판은 어느 크기에서도 정상이다.

## PixelLab — 승인 없이 절대 돌리지 않는다

**이 규칙을 지킨 적이 없어서 회원님이 여러 번 화를 내셨다. 다른 규칙보다 먼저 지킬 것.**

돌리기 전에 **반드시** 이 셋을 적어서 보여주고 **멈춘다**.

```
1. 프롬프트 전문
2. 크기와 파라미터
3. 비용(generation 수)과 남은 잔액
```

그리고 **"뽑아라 / 돌려라"에 해당하는 답을 받은 뒤에만** 호출한다.

**다음은 승인이 아니다. 이걸 승인으로 읽어서 사고가 났다.**

- 방식이나 방향에 대한 동의 — *"그렇게 하자", "1회씩 뽑는 걸로 가자"* 는 **방법론에 대한 동의**지
  지금 실행하라는 말이 아니다
- 앞선 승인 — 한 번 뽑아도 된다고 했다고 다음 것까지 되는 게 아니다. **매번 새로 받는다**
- 내가 물어본 뒤 회원님이 **다른 주제로 답한 경우** — 답을 못 받은 것이다. 다시 묻는다

과금은 환불되지 않는다. 애매하면 **안 돌린다.**

## PixelLab

**승인 없이 생성을 돌리지 말 것.** 프롬프트와 비용을 먼저 보여주고 허락을 받는다. 과금은
환불되지 않는다.

- `create_image_pixen`으로 **640x360 배경 한 장이 1 generation**이다. 변 768 이하, 4의 배수,
  면적 512x512(262,144px^2) 이내라는 조건에 640x360이 정확히 들어간다
- `create_image_pixflux`는 팔레트를 강제할 수 있지만 **400x400이 한계**라 배경에는 못 쓴다
- `create_image_pro`는 품질이 좋고 스타일 이미지를 참조할 수 있지만 **20~40 generations**다
- 배경 프롬프트에는 명암이 뚜렷한 조명을 넣는다. 계조를 4단으로 깎을 때 형태가 살아남으려면
  원본의 명암 차가 커야 한다

## 512는 최종 크기의 상한이 아니다 (2026-08-11)

출처: [인페인팅 튜토리얼](https://www.youtube.com/watch?v=q9z2Vhpz-Z8) — 영상에서 512x256 그림의
캔버스를 752x256으로 늘리고 빈 쪽을 이어 그린다.

> 전체 그림이 모델이 편집할 수 있는 크기보다 크면 그냥은 안 된다. 대신
> **"use selection tool to select painting area"** 로 제한 안에 드는 일부만 고르면, 모델은 그
> 영역만 처리하고 문서의 나머지는 그대로 둔다. **기존 그림 일부를 반드시 포함시켜라 —
> 모델이 이어 그리려면 맥락이 필요하다.**

**즉 512는 "한 번에 처리하는 영역"의 상한이지 만들 수 있는 그림의 상한이 아니다.** 캔버스를
크게 잡아두고 조각씩 이어 그리면 960x540짜리도 만들 수 있다.

다만 이것은 **웹 UI에서 손으로 하는 작업**이다. MCP의 `inpaint_image`는 입력이 512x512로 막혀
있어서, 자동으로 하려면 큰 캔버스를 512 조각으로 잘라 마스크와 함께 넣고 다시 이어붙여야 한다.

### 인페인팅 출력 방식 (용도가 다르다)

| | 언제 쓰나 |
|---|---|
| `new layer with changes` | **덧붙일 때.** 새 내용만 따로 나와 옮기고 지울 수 있다 |
| `new frame` | **바꾸거나 없앨 때.** 이걸 안 쓰면 지우려던 원본이 밑에 남는다 |
| `modify current layer` | 제자리에서 고칠 때 |

`crop_to_mask`를 켜면 칠한 영역 **바깥을 절대 안 건드린다.** 끄면 자연스럽게 섞으려고 주변
픽셀까지 조금 바꾼다.

**스프라이트 시트는 한 번에 편집한다.** 4방향 캐릭터의 같은 부위를 방향마다 칠하고 한 번
프롬프트하면 모든 방향이 일관되게 바뀐다.

## 타일셋 — 공식 영상에서 확인한 것 (2026-08-11)

출처: [Tilesets 튜토리얼](https://www.youtube.com/watch?v=sE1s5ZYfxqk) ·
[high top-down 튜토리얼](https://www.youtube.com/watch?v=q9z2Vhpz-Z8)

**도구가 둘이고 크기가 다르다.** 이걸 섞으면 안 된다.

| | 크기 | 타일 종류 |
|---|---|---|
| **high top-down 타일셋** (우리 것, MCP `create_topdown_tileset`) | **16 / 32** | 사각 탑다운만 |
| tiles 탭의 새 tile set (베타) | 32 / 48 / 64 / 96 | 아이소메트릭·오블리크·육각·사각 탑다운 |

### 값은 낮게 잡는다

영상에서 발표자가 실제로 쓰는 값이다. **일러스트 감각으로 높이면 32px에서 뭉개진다.**

```
outline          single color outline    ← 켠다. 작은 타일은 외곽선이 형태를 잡는다
detail           low detail
shading          basic shading
transition_size  0.5                     ← "대부분의 게임에서 보는 표준 벽"
묘사             두 단어 ("sand to water", "dungeon floor to lava")
```

`transition_size`는 **0.25가 아주 작은 벽, 0.5가 표준, 1.0은 큰 벽이지만 아직 베타라 불완전**하다.
길게 쓴 묘사(`polished pale marble floor with gold inlay`)는 32px에 들어갈 공간이 없어 평평한
색 덩어리가 된다.

### 같은 지형을 재사용해 게임 전체를 묶는다

`upper_base_tile_id` / `lower_base_tile_id`에 앞 타일셋의 타일 ID를 넣으면 **그 지형이 그대로
이어진다.** 영상 예시: 잔디→흙을 만든 뒤, 같은 잔디 타일로 잔디→물을 만든다. 환경이 달라져도
잔디는 하나다. **타일셋 ID는 버리지 말 것.**

### Godot으로 가져오는 법 — 코드가 필요 없다

MCP 자료(`pixellab://docs/godot/wang-tilesets`)에는 변환 스크립트를 쓰라고 나오지만, 영상은 훨씬
쉬운 길을 보여준다.

```
웹에서 "export as tileset 15"로 내보낸다
  ↓ TileMapDual 플러그인 (GitHub "dual grid godot" 검색 → zip → addons/ → 플러그인 켜기)
  ↓ TileMapDual 노드 → 새 TileSet → PNG 드래그 → "자동으로 타일 만들까?" 예
  ↓ TileMap 탭 → Terrains → 지형 두 개가 이미 잡혀 있다 → 바로 그린다
```

여러 타일셋을 같은 TileSet에 넣을 수 있다. 내보내기 형식은 wang(JSON) / **tileset 15** /
3x3(Godot 템플릿, 43타일)이 있고, 영상은 **tileset 15**를 쓴다.

### 소품은 맵 위에 인페인팅해서 만든다

맵을 조금 그린 뒤 그 위에 영역을 칠하고 `a house`, `an oak tree` 같이 적으면 **타일과 화풍이
자동으로 맞는** 소품이 나온다. 배경을 지우고 잘라내면 그대로 스프라이트다. 화풍 참조를 따로
넣을 필요가 없다 — 맵이 곧 참조다.

### 웹 UI 옵션 ↔ MCP 파라미터

| 웹 UI | MCP |
|---|---|
| Border Jitter / uneven boundaries | `raggedness` |
| Map Strength | `tileset_adherence` |
| Tile Strength | `tile_strength` |
| AI Border Freedom | `tileset_adherence_freedom` |
| terrain height | `transition_size` |
| edge steepness | `slope_size` / `spread_x` |

## 환경

- **Godot이 PATH에 없다**: `C:\Users\진로교육원\Desktop\Godot_v4.7.1-stable_win64.exe`
  (다른 컴퓨터에서는 `C:\Users\wlskr\OneDrive\바탕 화면\...`)
- **GUI 빌드라 콘솔에 출력이 안 나온다.** PowerShell에서 그냥 실행하면 기다리지도 않고
  로그도 없다. `Start-Process ... -Wait -RedirectStandardOutput`로 받아야 한다
- 새 파일을 넣었으면 `--headless --path <프로젝트> --import`를 먼저 돌려야 인식된다.
  프로젝트 실행만으로는 새 에셋을 스캔하지 않는다
- 컴파일/씬 로드 확인: `--headless --path <프로젝트> --quit-after 60`. 단 headless는 실제로
  그리지 않아서 **셰이더 오류는 안 잡힌다** — 셰이더는 창을 띄워서 확인해야 한다
- 이 PC에 쓸 수 있는 Python이 없다(스토어 껍데기만 있다). 이미지 처리는 Godot으로 한다
