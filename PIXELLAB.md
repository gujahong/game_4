# PixelLab 설명서

이 게임을 만들며 PixelLab에 대해 알아낸 것을 한곳에 모은다.

## 이 문서의 규칙

**출처에 우선순위가 있다.**

1. **공식 영상** — 가장 우선. 실제로 도구를 쓰는 모습이라 제일 정확하다
2. **공식 문서** (`pixellab.ai/docs`, MCP 스키마, `pixellab://docs/*`) — 수치와 한계
3. **우리 경험** — 맨 아래. 위 둘과 어긋나면 **위가 맞다**

우리가 직접 겪은 것은 §10에 따로 모아두고 본문에 섞지 않는다. 겪은 것이 틀린 짐작이었던 적이
여러 번 있었다.

---

## 1. 무엇을 만들 때 무엇을 쓰나

| 만들 것 | 도구 |
|---|---|
| 자유로운 그림(일러스트, 배경, 아이콘) | `create_image_pixen` (싸다) / `create_image_pro` (좋다) / `create_image_pixflux` (img2img·팔레트) |
| 탑다운 맵 지형 | `create_topdown_tileset` |
| 그 밖의 타일(아이소메트릭·육각·오블리크) | tiles 탭의 `tile set` (베타) |
| 맵 위의 소품 | `create_map_object` — **맵 조각을 참조로 넘기면 화풍이 맞춰진다** |
| 캐릭터 | `create_character` / `create_portrait_character` |
| 캐릭터 동작 | `animate_character` (템플릿) |
| UI | `create_ui_asset` |
| 글꼴 | `create_font` |
| 고치기 | `edit_image`(전체) / `inpaint_image`(일부만) |

---

## 2. 크기와 비용

### 자유 그림

```
create_image_pixen     변 16~768 · 4의 배수 · 면적 512x512(262,144) 이내   1 gen
create_image_pixflux   변 16~400 · 면적 400x400 이내                       1 gen
create_image_pro       512x512(정사각) / 688x384(16:9)                     20~40 gen
```

**정사각형 최대는 512x512다** — 면적 상한이 정확히 그 값이다.
**정확히 16:9인 최대는 640x360**이다(704x396은 면적 초과).
크기와 상관없이 pixen은 1 gen이므로 **작게 뽑는 것은 그냥 손해**다.

### 512는 최종 크기의 상한이 아니다

인페인팅 영상에서 512x256 그림의 캔버스를 752x256으로 늘리고 빈 쪽을 이어 그린다.

> 전체 그림이 모델의 편집 가능 크기보다 크면 **"use selection tool to select painting area"** 로
> 제한 안에 드는 일부만 고른다. 모델은 그 영역만 처리하고 나머지는 그대로 둔다.
> **기존 그림 일부를 반드시 포함시켜라 — 이어 그리려면 맥락이 필요하다.**

즉 512는 *한 번에 처리하는 영역*의 상한이다. 웹 UI에서 조각씩 이으면 더 큰 그림도 만들 수 있다.
(MCP의 `inpaint_image`는 입력이 512로 막혀 있어 자동화하려면 직접 잘라 넣어야 한다.)

### 타일·캐릭터

```
탑다운 타일셋      16 / 32px (standard)      64px는 pro         standard 1 gen
tiles 탭 tile set  32 / 48 / 64 / 96px       베타
캐릭터             최대 256px(v3) / 128px(standard·pro)
맵 오브젝트        32~400px · 화풍 참조 모드는 192x192 이내
```

**애니메이션 비용** (영상에서 명시)

```
pro 애니메이션     결과물 하나당 20~40 gen
V3 애니메이션      1~8 gen — 스프라이트 크기에 따라
템플릿             방향당 1 gen
```

**스프라이트가 작을수록 싸다.** 32x32 캐릭터는 V3로도 몇 gen 안 든다.

---

## 3. 화풍을 통일하는 법 ★

**이게 이 문서에서 제일 중요하다.**

### 답은 "생성할 때 잡는 것"이 아니라 "만든 뒤 팔레트로 강제하는 것"이다

출처: [How to Reduce Colors Like a Pro](https://www.youtube.com/watch?v=WSbBuAwRm18)

```
캐릭터를 만든다 → Pixelorama에서 열고 → cleanup → reduce colors → use palette
        ↓
   그 팔레트가 프로젝트 팔레트가 된다
        ↓
   애니메이션 · UI팩 · 타일셋 · 오브젝트에 전부 같은 팔레트를 적용한다
```

**서로 다른 도구로, 다른 날 만든 에셋이라도 같은 색만 쓰게 된다.**

`reduce colors` 도구(Pixelorama의 cleanup 안):
- 적용 범위: 현재 프레임 / 선택한 프레임 / **모든 프레임**(애니메이션은 이걸로)
- 출력: 새 레이어 / 현재 레이어 수정 / 새 프레임
- 방식: 자동 축소 / 색 개수 지정 / **팔레트 사용** ← 이것
- 팔레트는 **Lospec에서 링크나 이름으로 가져올 수 있다**
- **적용 전에 지저분한 픽셀을 먼저 치워라.** 깨끗한 입력이 결과가 좋다

타일셋도 된다 — Pixelorama에서 팔레트를 적용하고 갤러리에 저장하면 **맵 에디터가 자동으로
갱신된다.** 맵을 다시 그릴 필요가 없다.

### 생성 단계에서 거드는 것

- **`target palette`** — 애니메이션 도구(`animate to animation`)에는 팔레트를 **생성할 때부터
  강제하는 칸**이 있다. 안 고르면 도구가 알아서 색을 정한다. 팔레트가 정해져 있다면 여기서
  지정하는 것이 나중에 고치는 것보다 낫다
- **`use color palette from reference`** — 캐릭터 상태를 새로 만들 때 켜면 기존 캐릭터의 색을
  쓰면서 생성한다
- 같은 모델, 같은 시드, 같은 `view`/`outline`/`detail`, 비슷한 어휘 — 도움은 되지만 **보장은 안 된다**

### 참조 이미지로 화풍을 옮기는 것은 어렵다

- **`style_image`는 이미 픽셀아트여야 하고 32 / 64 / 128 / 256 중 한 크기**여야 한다
- **캔버스가 작을수록 참조를 많이 넣고 화풍을 많이 배운다** — 16·32→64장 / 64→16장 / 128→4장 /
  **256→1장**. 큰 그림을 뽑을수록 화풍 학습이 약해진다
- **`description`에 화풍을 적지 마라.** *"화풍은 이미 이미지에서 온다. 여기엔 뭘 만들지만 적어라"*
- **참조는 화풍만이 아니라 내용까지 복제한다.** 장면을 참조로 넣고 장면을 뽑으면 그 장면이
  거의 그대로 나온다
- `style_copy` / `style_options`로 팔레트·외곽선·디테일·명암 중 무엇을 가져올지 고를 수 있다

---

## 4. 자유 그림 (일러스트)

- `pixen`이 기본. 작은 스프라이트와 큰 캔버스 모두 무난하고 1 gen이다
- **가로로 긴 캔버스에서는 그림이 좌우로 갈라진다.** Pixen은 정사각형에 가까운 구도에
  최적화돼 있어서, 넓은 비율에서는 초점이 여러 개 생기거나 타일처럼 반복된다
- 넓은 장면을 하나로 얻으려면 `create_image_pro`(688x384)뿐이다. 다만 20~40 gen
- 예산이 빠듯하면 **넓은 배경은 pixen으로, 주제는 정사각형+투명배경으로 따로 뽑아 엔진에서
  합치는 것**이 공식 권장이다
- 빈자리를 안 채워주면 모델이 **주제를 복제해서** 채운다. 좌우에 무엇이 있을지 말해줄 것

---

## 5. 타일셋과 맵

출처: [Tilesets 튜토리얼](https://www.youtube.com/watch?v=sE1s5ZYfxqk) ·
[high top-down 튜토리얼](https://www.youtube.com/watch?v=q9z2Vhpz-Z8)

### 도구가 둘이고 크기가 다르다

| | 크기 | 종류 |
|---|---|---|
| high top-down 타일셋 (MCP `create_topdown_tileset`) | 16 / 32 | 사각 탑다운 |
| tiles 탭의 tile set (베타) | 32 / 48 / 64 / 96 | 아이소메트릭·오블리크·육각·사각 탑다운 |

### 값은 낮게 잡는다

영상에서 실제로 쓰는 값이다. 일러스트 감각으로 올리면 작은 타일에서 뭉개진다.

```
outline          켠다 (single color outline)
detail           low detail
shading          basic shading
transition_size  0.5      ← "대부분의 게임에서 보는 표준 벽"
묘사             두 단어   "sand to water" / "dungeon floor to lava"
```

`transition_size`: **0.25 아주 작은 벽 · 0.5 표준 · 1.0 큰 벽이지만 아직 베타라 불완전**.

### 같은 지형을 재사용해 게임 전체를 묶는다

`upper_base_tile_id` / `lower_base_tile_id`에 앞 타일셋의 타일 ID를 넣으면 그 지형이 그대로
이어진다. 잔디→흙을 만든 뒤 같은 잔디로 잔디→물을 만드는 식이다. **타일셋 ID를 버리지 말 것.**

### 맵 에디터

- **오토타일**이 코너와 전환 조각을 자동으로 고른다. 끄면 특정 타일을 직접 고를 수 있다
- **stack layer** — 높이 층을 나눠 위에 얹는다
- **brush height** — 칠하면서 타일을 세로로 쌓는다(벽·단)
- **add tile group** — 같은 종류의 타일셋을 한 맵에서 같이 쓴다
- **inpaint map** — 맵 위에 칠하고 `stairs`라고 쓰면 지형에 이어지는 계단이 생긴다
- **create object** — 같은 방식이지만 **움직일 수 있는 물건**으로 나온다. 배치·복제 가능
- **edit in Pixelorama** — 생성 결과를 손으로 정리한다. *영상 내내 "clean it up"을 반복한다 —
  AI 결과를 그대로 쓰는 워크플로가 아니다*

---

## 6. 소품

- `create_map_object`에 **맵 화면 조각(192x192 이내)** 을 `background_image`로 넘기면 그 맵의
  화풍에 맞춰 그려준다. 인페인팅이라 배경이 복제되지 않는다
- 웹에서는 맵 위에 직접 인페인팅하는 쪽이 더 쉽다 — 맵이 곧 참조다
- 8방향 오브젝트는 완성된 8방향 오브젝트를 `style_object_id`로 넘겨 화풍을 맞춘다

---

## 7. 인페인팅

출처: [Inpainting 튜토리얼](https://www.youtube.com/watch?v=9eAlZlQmZBg)

### ★ 비용을 먼저 볼 것 — 1 gen이 아니다

MCP 도구 설명에 박혀 있다(2026-08-11 확인).

```
inpaint_image   20~40 generations   (Gemini tier, 이미지 크기에 따라)
edit_image      20~40 generations   (프레임 격자 전체 기준)
```

**`create_image_pro` 한 장과 같은 값이다.** 그래서 "512 그림의 좌우를 인페인팅으로 늘려서 넓은
그림을 만든다"는 40~80 gen이 되어, **pro로 688x384를 한 번에 뽑는 것보다 두 배 이상 비싸다.**
§2의 "512는 상한이 아니다"는 여전히 맞지만, **그건 웹 UI에서 손으로 할 때 얘기다.**

`edit_image`는 프레임 격자 전체로 값을 매기므로 **여러 프레임을 한 번에 넣는 것이 이득**이다
(64x64 하나 고치는 값이나 열여섯 개 고치는 값이나 같다).

### ★ 캔버스를 늘려 이어 그리기 (아웃페인팅) — 영상의 절차 그대로

**된다.** 우리가 60 gen을 태우고도 못 했지만 그건 절차를 틀린 것이다(§10 참고).

영상은 **512x256 환경 그림을 752x256으로** 늘려 오른쪽을 이어 그린다. 순서는 이렇다.

```
1. 캔버스를 늘린다 (오른쪽에 빈 공간이 생긴다)
2. 그냥 프롬프트 넣고 생성하면 → 도구가 거부한다
   "전체 이미지가 모델이 편집할 수 있는 크기보다 크기 때문"
3. ★ "use selection tool to select painting area" 옵션을 켠다
4. 선택은 **크게** 잡되 모델 크기 제한 안에 들게 한다
   ★ 기존 그림 일부 + 새 빈 공간을 **둘 다 포함**해야 한다
     - "이어 그리려면 맥락이 필요하기 때문"
5. 인페인팅 마스크는 **빈 쪽에만** 칠한다
6. 프롬프트는 채울 내용만 (영상 예: "river buildings and garden")
7. 생성 후 손으로 정리한다 - 튄 픽셀, 원근, 색
```

**3번이 핵심이다.** 이 옵션이 켜져야 모델이 선택 영역만 처리하고 문서의 나머지는 그대로 둔다.
문서를 새로 만들거나 손으로 쪼갤 필요가 없다.

> 배경·맵·풍경·벽지처럼 큰 게임 환경을 **전체를 한 번에 처리하지 않고** 넓히는 좋은 방법이다.

### 출력 방식이 용도를 가른다

| | 언제 |
|---|---|
| `new layer with changes` | **덧붙일 때**(선글라스). 새 내용만 따로 나와 옮기고 지울 수 있다 |
| `new frame` | **바꾸거나 없앨 때**. 이걸 안 쓰면 지우려던 원본이 밑에 남는다 |
| `modify current layer` | 제자리에서 고칠 때 |

- **`crop to mask`** — 켜면 칠한 영역 바깥을 절대 안 건드린다. 끄면 자연스럽게 섞으려고 주변까지
  조금 바꾼다
- **스프라이트 시트를 한 번에 편집한다.** 4방향 캐릭터의 같은 부위를 방향마다 칠하고 한 번
  프롬프트하면 모든 방향이 일관되게 바뀐다
- 큰 그림은 **선택 도구로 일부만** 골라 처리한다(§2 참고)

---

## 8. 캐릭터와 애니메이션

출처: [Generating pixel art characters and animations](https://www.youtube.com/watch?v=ptWw9gkgorQ)

### 만드는 순서

```
캐릭터 설명을 쓴다 → 크기·시점·회전수·모양을 정한다 → 생성 → 허브에 들어간다
                                                          ↓
                                        캐릭터를 고른 상태에서 애니메이션을 붙인다
```

**애니메이션은 캐릭터를 만든 뒤에만 붙는다.** 캐릭터 없이 동작만 뽑을 수 없다.

### 설정

| | |
|---|---|
| **설명** | 구체적으로. *"brave knight in golden armor, red scarf, short cape, leather boots"* — 색·옷·특징·양식을 적는다. **자세할수록 좋다** |
| **캔버스** | 48 / **64(기본)** / 96 / 128 / 160. 64가 *"복고풍과 가독성의 균형"*, 48은 더 뭉툭, 96 이상은 더 세밀 |
| **시점** | low top-down / **high top-down(고전 RPG 느낌)** / sidescroller |
| **회전 수** | 8방향이 기본(대각선 포함) |
| **AI freedom** | **낮게 둔다.** 낮을수록 설명을 그대로 따른다 |
| **외곽선 / 명암 / 디테일** | 영상은 selective outline · basic shading · medium detail |
| **비율** | 머리 크기 · 어깨 너비 · 다리 길이를 직접 조절하거나 **프리셋**(chibi / cartoon / heroic / realistic) |

**설정 중에 보이는 회색 인형은 미리보기일 뿐 최종 결과가 아니다.** 시점과 비율만 반영된다.

### 애니메이션

- 캐릭터를 고른 상태에서 붙인다. 예: running(8프레임) / cross punch / getting up
- **백그라운드로 돌려놓고 다른 애니메이션을 계속 만들 수 있다** — 하나씩 기다릴 필요가 없다
- 템플릿에 V3 계열이 있고 **idle / walking / running / full sprint**를 지원한다. 그전에는
  캐릭터 상태를 만들어 custom animation V3로 조합했다
- 템플릿 애니메이션은 **방향당 1 gen**

### 허브와 손보기

- 모든 캐릭터가 회전·애니메이션과 함께 **허브**에 저장된다
- 내려받을 때는 **스프라이트시트**로, 작업한 크기를 골라 받는다
- **`edit in Pixelorama`** — 웹에서 바로 열려서 고치고 저장하면 캐릭터 페이지로 돌아간다.
  **내려받고 다시 올릴 필요가 없다**
- *"자동 생성 스프라이트는 시간을 크게 아껴주지만 프레임에 튄 픽셀이 섞이는 건 정상이다.
  특히 여러 프레임 애니메이션에서. PixelLab이 90%까지 데려다주고 나머지 10%가 당신 손이다"*

### 동작을 만드는 두 도구 — animate with text / interpolate

출처: [How to Use Animate with Text](https://www.youtube.com/watch?v=j2bE9gq914U)

```
animate with text   그림 한 장 + 동작 설명  →  움직임을 만들어낸다
interpolate         첫 프레임 + 끝 프레임    →  그 사이를 채운다
```

**공통 — `enhance prompt`를 눌러라.** 모델에게 움직임·타이밍·힘·전환에 대한 정보를 더 준다.
영상에서 안 누르고 뽑은 결과는 눈에 띄게 단순했다.

#### animate with text

- 프레임 수는 6이 기본, 길게 전개할 것은 16
- **캔버스가 클수록 프레임 상한이 낮다** — 256x256에서는 최대 8프레임이었다
- 장면 전체를 살리려면 `remove background`를 끈다
- **참조 그림의 자세가 그 동작에 이미 맞아야 한다.** 달리는 자세를 주고 "walk loop"를 시키면,
  달리기 → 걷기 → 다시 달리기로 돌아와야 해서 결과가 나빠진다
- 캐릭터만이 아니라 **물건·초상·장면·세로 그림**에도 된다(상자가 열리며 우주가 보이고,
  노인이 오크로 변하고, 256x256 전투 장면에서 망치를 내리친다)
- **★ 이어 붙이기: 한 애니메이션의 마지막 프레임을 다음 참조로 쓴다.** 변신 → 웃음,
  주문 → 마무리 일격처럼 여러 단계짜리 연출을 이렇게 쌓는다
- 일부만 움직이게 하려면(모닥불의 불꽃만) **첫 프레임의 정지 부분을 나머지 프레임에 복사**한다

#### interpolate

- 시작과 끝을 내가 정하므로 **포즈를 이미 알고 있고 그 사이만 필요할 때** 좋다
- **같은 그림을 시작과 끝에 둘 다 넣으면** 그 자세를 유지하는 반복 동작이 된다
- 변신에도 쓴다 — 캐릭터 A→B, 잎이 무성한 나무→불타 버린 나무, 낮 장면→비 오는 밤

#### animate to animation

출처: [Animation to animation](https://www.youtube.com/watch?v=owkamgYVWAs)

칸이 이렇게 나뉜다.

| | |
|---|---|
| **number of images** | 이번 생성으로 뽑을 프레임 수. **캔버스 크기에 따라 상한이 달라진다** |
| **animation reference** | 참조 이미지, 또는 시작점을 지정할 `init image` |
| **description** | 무엇을(캐릭터·그림) |
| **action description** | 무슨 동작을 — **설명 칸이 둘로 나뉘어 있다** |
| **camera view / direction** | |
| **outline · shading · detail** | |
| **target palette** | **팔레트를 여기서 강제할 수 있다.** 안 고르면 도구가 알아서 색을 정한다 |
| 고급: **AI freedom · robustness · guidance weight** | 참조·설명을 얼마나 따를지 vs 자유롭게 갈지 |

**★ 긴 동작을 이어갈 때**: 마음에 드는 결과가 나오면 **그것을 참조 이미지로 지정하고** 다음
프레임들을 생성한다. 그래야 시퀀스 내내 일관성이 유지된다.

> 손으로 정리할 부분은 남는다. 다만 도구가 **90%까지는** 데려다준다.

#### 캐릭터 창에서 하는 것이 낫다

포즈와 상태를 스프라이트에 손으로 정리하지 말고 **캐릭터 창에서 상태를 만들고, 거기서
animate with text와 interpolate를 쓰라**고 영상이 권한다. 애니메이션이 여러 개거나 8방향을
다룰 때 특히 그렇다.

### 실전 순서 — 게임 한 편을 통째로 만든 사례

출처: [GBA-Style Sprites](https://www.youtube.com/watch?v=moCpjMOOBGk) — 젤다풍 탑다운 게임의
주인공·적·NPC·타일셋·소품·UI를 전부 PixelLab으로 만든다. **우리 게임과 가장 가까운 사례다.**

```
1. 캐릭터를 뽑는다            32x32 · high top-down · 8방향
2. ★ 애니메이션 전에 먼저 정리한다   edit in Pixelorama → 튄 픽셀 정리 → 저장
3. 걷기·달리기는 템플릿        walk 6프레임
4. 없는 동작은 custom animation V3
5. 적·NPC는 주인공을 참조로     create from style reference pro
6. 타일셋 → 맵 → 소품 → UI
```

**2번이 중요하다.** *"애니메이션에 손대기 전에 완벽한 바탕이 필요하다"* — 정지 스프라이트가
지저분하면 그 오류가 모든 프레임에 복제된다.

### generation을 아끼는 요령

- **★ 절반만 뽑고 뒤집는다.** 남·남동·동·북동·북만 생성하고, 동/남동을 **좌우 반전**해서
  서/남서로 쓴다. 8방향 중 3방향을 아낀다
- **작게 뽑으면 한 번에 더 많은 후보가 나온다.** 32x32는 배치당 생성 수가 더 많아 고를 게 많다
- `create_image_pro`는 **한 번에 여러 프레임을 시트로** 준다. 화살표 8방향 같은 건 하나 뽑아
  손으로 돌려 쓰는 편이 싸다

### 걸어본 함정

- **★★ 걷기 애니메이션은 서 있는 프레임에서 시작하지 마라.** 이미 **다리를 벌린 중간 자세**를
  시작 프레임으로 쓰면 훨씬 자연스럽다. 한 번 뽑아서 중간 프레임을 찾은 뒤, 그것을 시작점으로
  다시 뽑는다 (Discord에서 나온 요령이라고 한다)
- **물건을 든 캐릭터에는 템플릿을 쓰지 마라.** 템플릿은 대개 **맨손 캐릭터로 학습**돼서 검을 든
  기사에 쓰면 이상해진다. `custom animation V3`를 쓸 것
- **★ 크기가 어긋나는 이유는 참조 그림의 캔버스 크기다.** 56x56 캔버스에 담긴 32x32 캐릭터를
  참조로 넣었더니 생성된 적이 더 크게 나왔다. **캔버스를 캐릭터 크기에 맞춰 줄여서** 넣을 것
- 얼굴이 프레임마다 흔들리면, **정지 프레임의 깨끗한 머리를 복사해 애니메이션 프레임에 붙인다**
- 소품이 너무 크게 나오면 **칠하는 영역을 조금 지워서** 줄인다

### 타일셋을 더 예쁘게 고치는 법

생성된 타일셋이 밋밋하면 **`edit in Pixelorama` → `edit image pro`** 로 고칠 수 있다.
영상에서는 *"make the grass and water prettier, more muted, no gradients"* → 다시
*"have some texture"* 를 얹고 맵 워크숍에 저장한다. **타일셋을 다시 뽑지 않고 고친다.**

타일셋 전환은 **50%가 대개 제일 낫다**고 여기서도 말한다(§5와 일치).

### 새로 생긴 편의

회전 페이지에서 **`create character` 버튼**을 누르면 프레임을 하나씩 끌어다 넣을 필요 없이
캐릭터 창으로 한 번에 들어가고 골격까지 잡아준다.

### 다른 방법

8방향 스프라이트를 `create 8-directional sprite pro`로 먼저 만들고, `create character`의
**standard** 옵션으로 올릴 수도 있다(이미 8방향이 있으니 다시 회전시킬 필요가 없다).
`reduce colors` 워크플로가 이 경로를 쓴다(§3).

pro 모드 캐릭터는 `n_directions` / `outline` / `shading` / `detail` / `text_guidance_scale` /
`proportions`를 **무시한다**.

---

## 9. Godot으로 가져오기

### 타일셋

**쉬운 길(영상)** — 코드가 필요 없다.

```
웹에서 "export as tileset 15"로 내보낸다
  ↓ TileMapDual 플러그인 (GitHub "dual grid godot" → zip → addons/ → 플러그인 켜기)
  ↓ TileMapDual 노드 → 새 TileSet → PNG 드래그 → "자동으로 타일 만들까?" 예
  ↓ TileMap 탭 → Terrains → 사각형 도구(R)로 그린다
```

내보내기 형식은 wang(JSON) / **tileset 15** / 3x3(Godot 템플릿, 43타일). 영상은 tileset 15를 쓴다.

**MCP로 받았을 때** — 16타일 wang 시트 + metadata JSON이 온다. 타일 자리는 metadata의
`bounding_box`로 잘라낸다. `original_position`과 `wang_N` 이름은 **시트 좌표가 아니다**(쓰면
가로줄 무늬가 생긴다). 코너 번호는 `NW*8 + NE*4 + SW*2 + SE`(위 지형=1).

**주의**: 25타일 전환 세트(`transition_size` 1.0 등)는 Godot의 코너 기반 지형으로 표현할 수 없다.
16타일을 유지할 것.

### 프로젝트 설정

`Project Settings → Rendering → Textures → Default Texture Filter = Nearest`.

---

## 10. 우리 경험 (영상·문서에 없거나 어긋나는 것)

**여기 있는 것은 확인된 사실이 아니라 우리가 겪은 일이다.** 위 절과 충돌하면 위가 맞다.

- **작은 타일에 일러스트 파라미터를 쓰면 망한다.** 32px 타일에 `highly detailed` + `lineless`를
  주니 평평한 색 덩어리가 나왔다. 영상 값(`low detail` + 외곽선 켬)으로 바꾸니 바로 나아졌다
- **시드와 파라미터를 고정해도 화풍이 안 잡힌다.** 같은 설정(seed 1000, lineless, highly
  detailed)으로 뽑은 성당과 피라미드가 딴판이었다. 소재가 화풍을 끌고 간다
- **투명 배경에 물건 하나만 뽑으면 아기자기해진다.** 공기와 비교 대상이 없어 게임 에셋처럼
  보인다. 장면으로 뽑아야 분위기가 산다
- **프롬프트로 "화면 밖으로 잘리게"는 잘 안 먹는다.** 주어를 바꿔야 한다 — "탑"이 아니라
  "탑의 밑동". 하늘 소재를 넣으면 카메라가 물러난다
- `symmetrical`은 좌우 거울 복제를 부른다
- **승인 없이 생성을 돌리지 말 것** — `CLAUDE.md`의 규칙을 볼 것

### ★ 아웃페인팅으로 60 generations을 태웠다 (2026-08-12)

512x512 관문 그림의 캔버스를 644x512로 늘려 오른쪽을 이어 그리려다 **세 번 실패하고 60 gen을
날렸다.** 원인은 하나다.

> **`use selection tool to select painting area` 옵션을 안 켰다.**

이 옵션이 꺼져 있으면 도구가 **원본 512 범위만 처리하고 새로 늘린 부분은 아예 안 본다.** 세 번 다
생성 레이어에 원본 안쪽의 작은 조각만 들어 있었고, 빈 자리는 그대로였다. 그런데도 거부되지 않고
**20 gen씩 정상 청구된다** — 실패를 알려주지 않으므로 결과를 반드시 눈으로 확인할 것.

화면에 보이는 **`Paint in selection`은 이것과 다른 항목일 수 있다.** 영상이 말하는 이름으로 찾을 것
(`Advanced options` 안에 있을 가능성이 있다. 우리는 끝내 그걸 펼쳐보지 않았다).

**이 절차는 §2와 §7에 처음부터 적혀 있었다.** 읽고도 적용하지 않아서 태운 값이다. 다음에 인페인팅을
쓸 때는 **돌리기 전에 §7의 7단계를 한 줄씩 짚어가며 확인할 것.**

곁다리로 확인된 것들:

- **인페인팅·edit_image는 20~40 gen이다.** 1 gen이 아니다(§7 맨 위)
- 공식 문서의 "tier 1 인페인팅 최대 100x100"은 이 경로에는 해당되지 않는 것으로 보인다 —
  그보다 훨씬 큰 영역을 UI가 거부 없이 받아들였다
- **웹 UI는 생성 후 시드를 알려주고 `Reuse seed` 버튼을 준다.** MCP는 시드를 안 돌려주므로,
  재현이 필요하면 **호출할 때 시드를 직접 지정해야 한다**
