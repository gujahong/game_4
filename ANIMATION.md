# 애니메이션·도트 설명서

**도트 애니메이션을 만드는 법**과 **그림을 코드로 찍는 법**, 그리고 **그 일을 클로드에게
시키는 법.** 움직이고 흩어지는 연출(셰이더·트윈·파티클)은 `EFFECT.md`가 다루고, 여기는
**그림 자체** — 프레임을 그리는 것과 픽셀을 놓는 것이다.

## 이 문서의 규칙

**출처에 우선순위가 있다.**

1. **공식 문서** — Godot 문서. 함수 이름과 동작
2. **업계 관행** — 도트 작가들의 강좌(Slynyrd 등). 프레임 수와 박자
3. **우리 경험** — 맨 아래(§7). 위와 어긋나면 **위가 맞다**

**확인 안 된 것은 "확인 안 됨"이라고 적는다.**

---

## 1. 도트 애니메이션의 기본 — 프레임은 적게, 키프레임은 세게

도트 작가들의 강좌가 입을 모으는 것이 하나다. **"부드러움 ≠ 좋은 애니메이션."**

> "프레임이 너무 많으면 움직임에서 힘이 빠진다. 키프레임 설계에 힘을 쏟아라." — Slynyrd

### 프레임 수의 상장

| 동작 | 프레임 | 한 장을 보여주는 시간 |
|---|---|---|
| 숨쉬기(대기) | 2~4장 | 300~500ms |
| 걷기 | 4~6장 | 100~150ms |
| 달리기 | 6~8장 | 60~100ms |
| 공격 준비(들어올림) | 1~2장 | 60~80ms |
| **공격 맞는 순간** | 1장 | **120~200ms — 오래 잡는다** |
| 불·이펙트 | 3~8장 | 100~300ms |

8비트 JRPG는 **2장짜리 걷기를 대기에도 돌려썼다.** 적은 프레임이 부끄러운 게 아니라 그 화풍의
일부다 — 3장짜리 대기를 300ms로 돌려도 살아 있는 것으로 읽힌다.

### ★ 박자를 고르게 주지 않는다

한 장 한 장에 **다른 시간**을 준다. 이것이 프레임을 더 그리는 것보다 싸고 세다.

```
준비 80ms → 준비 80ms → 휘두름 60ms → ★ 맞는 순간 150~200ms → 되돌아옴 80ms
```

**맞는 순간을 오래 잡는 것**이 아마추어와 프로를 가른다고들 한다. 우리 `_enemy_struck()`의
`STRUCK_HOLD`가 코드로 같은 일을 한다.

### 키프레임 먼저, 사이는 나중에

- **키프레임**: 동작의 정수가 담긴 제일 극적인 자세. 이것부터 그린다
- **사이 프레임(인비트윈)**: 키프레임 사이를 잇는 장. 걷기라면 닿는 발과 지나가는 발이
  키프레임이고, 나머지는 있어도 그만이다
- 불·물·연기 같은 **유기적인 것만 첫 장부터 차례로** 그린다(스트레이트 어헤드)

---

## 2. 도트에서 중요한 원칙 몇 가지

디즈니의 12원칙 중 도트에서 특히 값어치 있는 것만. 숫자는 도트용으로 번역된 것이다.

| 원칙 | 도트에서는 |
|---|---|
| **찌그러짐·늘어남** | 14px짜리가 뛰면 15~16px로 늘고, 닿으면 12~13px로 찌그러진다. **부피는 지킨다** — 높이를 줄이면 폭을 1px 더한다 |
| **예비 동작** | 본동작 전에 **반대 방향으로 한 장.** 치기 전에 당기고, 뛰기 전에 웅크린다 |
| **실루엣** | 뭉개 봐도 동작이 읽혀야 한다. 16px에서는 **읽힘이 사실성을 이긴다** |
| **따라오는 것** | 몸이 멈춘 뒤 머리카락·망토가 1px 더 간다. 이 1px이 살아 있음을 만든다 |
| **과장** | "맞다 싶은 것보다 30% 더 밀어라. **작은 그림에서 사실적으로 보이면 너무 약한 것이다**" |
| **호(arc)** | 휘두르는 것은 직선이 아니라 곡선을 따라 간다 |

### 스미어(뭉갬) 프레임

빠른 동작의 중간에 **형태를 뭉개서 궤적으로 만든 한 장**을 끼운다. 도트의 모션 블러다.
칼이 한 장에서는 칼이고 다음 한 장에서는 **호를 그리는 색 띠**가 된다 — 게임 속도로 보면
눈이 속는다. **공격 하나에 스미어 한 장**이면 충분하다고 한다.

### 이펙트(폭발·타격 불꽃)의 공식

```
팽창 → 흩어짐 → 사라짐
```

세 단계면 된다. 먼지는 3장(작게 → 크게 → 흐려짐), 타격 불꽃은 2장(번쩍 → 흩어짐)짜리
예가 흔하다. **처음이 제일 크고 밝고, 뒤로 갈수록 잘게 흩어진다.**

---

## 3. Godot에서 돌리는 법

| | 언제 |
|---|---|
| **`AnimatedSprite2D` + `SpriteFrames`** | 그림 장수만 넘기면 될 때. **우리 순례자가 이것**(`HeroSprite.gd`, `_frames.gd`가 엮는다) |
| **`AnimationPlayer`** | 여러 노드의 속성·함수 호출·소리를 **한 타임라인**에 얹을 때. 특정 프레임에서 함수를 부르는 트랙(Call Method Track)이 있다 |
| **트윈** | 코드에서 즉석으로. 우리 전투 연출이 전부 이것 |

우리는 `AnimationPlayer`를 **한 번도 안 썼다.** 연출을 전부 코드(트윈)로 짜는 방식과 맞물려
있어서인데, "발이 땅에 닿는 프레임에 발소리"처럼 **그림 장수와 사건을 묶는 일**이 생기면
그때가 `AnimationPlayer`를 꺼낼 때다. (Call Method Track은 에디터 미리보기에서는 안 불리고
실행 중에만 불린다)

`SpriteFrames`는 `_frames.gd`가 코드로 엮는다 — 방향별 폴더의 PNG를 모아 `.tres` 하나로.
**0번은 서 있는 기준 프레임이고 1번부터가 걷기다**(PixelLab이 원본을 첫 장에 끼워 주므로).

---

## 4. 코드로 도트를 찍는 법 (`Image`)

`tools/`의 그림 도구들이 전부 이 API 위에 있다. **0원이고 몇 번이든 다시 돌릴 수 있다.**

```gdscript
var image := Image.create_empty(W, H, false, Image.FORMAT_RGBA8)
image.set_pixel(x, y, color)              # 한 점
image.fill_rect(Rect2i(...), color)       # 네모
image.blit_rect(src, rect, at)            # 그대로 복사 (알파 무시)
image.blend_rect(src, rect, at)           # 알파를 섞으며 복사
image.crop(w, h)  image.flip_x()          # 자르기·뒤집기
image.resize(w, h, Image.INTERPOLATE_NEAREST)   # ★ 늘릴 일이 있으면 반드시 NEAREST
image.save_png(path)
```

읽을 때는 `Image.load_from_file(path)` 뒤에 `convert(Image.FORMAT_RGBA8)`를 한 번 —
포맷이 다른 채로 `get_pixel` 하면 엉뚱한 값이 나온다.

화면에 즉석으로 올리려면 `ImageTexture.create_from_image(image)`.

### 우리가 찍어본 것에서 나온 규칙

`_lantern_big.gd`(등불) · `_silhouette.gd`(순례자 실루엣) · `_hood.gd` 등을 만들며 굳은 것이다.

1. **놓일 크기 그대로 찍는다.** 작게 찍어 늘리면 도트가 커질 뿐 **디테일이 생기지 않는다** —
   8×11 등불을 두 배로 늘려봐야 도트 하나가 여덟 칸이 될 뿐이다. 두 배 크기가 필요하면
   **두 배 크기로 새로 그린다.** 칸이 네 배가 되면서 뭉개졌던 것들이 자리를 얻는다
2. **대칭인 물건은 왼쪽 절반만 찍고 거울로 뒤집는다.** 좌우를 따로 찍으면 반드시 어긋나고,
   정면 물건은 그 어긋남이 바로 보인다 (`_lantern_big.gd`의 `_row()`)
3. **색은 늘리지 않는다.** 등불이 일곱 색이다. 크기가 네 배가 될 때 **두 색만** 더했다 —
   빛 받는 면과 그늘 면. 색이 많아지면 게임의 화풍에서 그 물건만 튄다
4. **빛의 방향을 정하고 시작한다.** 등불은 "위가 밝다"로 잡았다 — 제 빛으로 제 갓을 못
   비추므로. 방향 없이 칠하면 물건이 평평해진다
5. **사람으로 읽히게 하는 단서는 하나면 된다.** 실루엣에서 **목의 잘록함** 하나가 종 모양과
   사람을 가른다 (`_silhouette.gd`)

### 코드로 찍기 좋은 것과 나쁜 것

```
좋다    실루엣(윤곽만 맞으면 됨) · 대칭 소품(등불) · 격자가 있는 것(창살·타일)
        규칙이 있는 것(지도, 팔레트 표) · 기존 그림의 가공(채도, 자르기, 뒤집기)
나쁘다  얼굴 · 천의 주름 · 유기적인 곡선 · 붓 맛이 필요한 것 → PixelLab이나 손
```

---

## 5. 도트 애니메이션의 두 가지 길

### 길 1 — 프레임을 그려서 넘긴다 (정석)

PixelLab의 `animate_character`(우리 순례자 걷기가 이것), 또는 손으로 Aseprite·Pixelorama에서.
**Pixelorama는 무료·오픈소스에 Godot로 만들어져 있고** 어니언 스킨과 프레임 태그가 있다.
Aseprite를 쓰면 `Aseprite Wizard` 애드온이 `SpriteFrames`까지 만들어 넣어준다. (안 써봤다)

### 길 2 — 그리지 않고 움직인다 (우리가 주로 하는 것)

한 장을 코드가 움직인다 — 트윈으로 밀고, 셰이더로 흩고, `_draw()`로 다시 그린다.
`Pages.gd`의 낱장이 이 방식의 표본이다: **그림은 흰 네모 하나**인데, 폭을 `sin`으로 줄였다
늘리는 것만으로 팔랑이는 낱장이 된다.

**가르는 기준** — 형태가 변하면 길 1, 형태는 그대로고 **자리·기울기·투명도만** 변하면 길 2.
걷는 사람은 다리 모양 자체가 변하니 길 1이고, 흔들리는 등불은 길 2다.

길 2를 쓸 때도 도트로 보이게 하는 요령이 있다 (`EFFECT.md` §2와 같은 이야기):

- 자리는 **정수 픽셀로 끊는다** (`.round()`)
- 시간도 끊는다 — 60fps로 부드럽게 움직이면 도트 그림과 결이 어긋난다. 초당 8~12번만
  값을 바꾸면 손으로 찍은 것처럼 보인다 (확인 안 됨 — 우리는 아직 트윈을 매끄럽게 쓴다)

---

## 6. ★ 클로드에게 그림·애니메이션을 시키는 법

`EFFECT.md` §8과 짝이다. **그림에서는 갈림이 더 심하다.**

| | |
|---|---|
| **잘한다** | 규칙이 있는 그림을 코드로 찍는 것 (§4의 "좋다" 목록) |
| **잘한다** | 그림의 가공 — 자르기, 채도, 팔레트 뽑기, 여백, 이어붙이기 |
| **잘한다** | PixelLab에 보낼 프롬프트를 다듬는 것 |
| **못한다** | **미적 판단.** 예쁜지 아닌지 나는 모른다. 회원님이 보고 말해줘야 한다 |
| **못한다** | 유기적인 형태를 좌표로 찍는 것. 얼굴을 `set_pixel`로 찍으면 반드시 이상하다 |

### 그림을 확인시키는 법

작은 도트 그림은 **그대로는 나도 못 본다** — 16×22짜리를 읽어봐야 점 몇 개다.
`tools/_zoom.gd`가 이걸 위해 있다: 정수배로 확대해 PNG로 저장하면 내가 읽고 판단할 수 있다.
여러 장을 주면 가로로 이어붙여 비교도 된다 (`_font_compare.png`가 그렇게 나왔다).

```
찍는다 → _zoom.gd로 확대 → 내가 읽는다 → 고친다 → 다시
```

이 고리가 소리의 `_probe_sfx.gd`, 이펙트의 `--shot`과 같은 자리다. **세 감각 모두 같은
교훈이다 — 짐작으로 고치지 말고 재서/찍어서 고친다.**

### 세상에 있는 다른 방법들 (우리는 안 쓴다)

- **Claude + Aseprite MCP** (`pixel-plugin` 등) — 클로드가 Aseprite를 원격 조종해 그리게 하는
  물건이 여럿 있다. 결국 클로드가 좌표를 찍는 것이라 §4의 한계가 그대로다. Aseprite가
  없는 우리에게는 이점이 없다
- **파이썬 Pillow로 찍는 스킬** — 우리 `Image` 방식과 같은 일을 파이썬으로 하는 것.
  Godot 안에서 찍는 우리 쪽이 임포트 한 단계가 덜 든다
- **절차적 스프라이트 생성기** — 반쪽 난수 + 거울 대칭으로 우주선 따위를 무한히 뽑는
  장르가 있다(pixel-sprite-generator). **거울 대칭이 "그럴듯함"의 핵심**이라는 점은 우리
  등불과 같은 발견이다

### 시킬 때의 요령

- **참조를 보여줄 것.** "등불 그려줘"보다 기존 그림 파일 경로를 주고 "이 화풍으로"가 낫다.
  나는 프로젝트의 PNG를 읽을 수 있다
- **팔레트를 먼저 정하게 할 것.** 색 상수 목록이 코드 맨 위에 있으면(우리 도구들처럼)
  고치기도 화풍 맞추기도 쉽다
- **좌표에 "왜"를 적게 할 것.** `_row(image, 6, 7, 0, FRAME)`만 있으면 다음에 아무도 못
  고친다. "손잡이 고리. 위로 솟은 반원이 손에 드는 물건이라는 제일 강한 표시다" 같은
  주석이 이 도구들을 다시 쓸 수 있게 한다

---

## 7. 우리 경험

**여기 있는 것은 확인된 사실이 아니라 우리가 겪은 일이다.** 위 절과 충돌하면 위가 맞다.

- **늘려서는 디테일이 안 생긴다.** 등불을 두 배로 키울 일이 생겼을 때 `resize`가 아니라
  **새로 그리는 것**이 답이었다 → §4-1
- **거울 대칭이 손으로 맞춘 대칭을 이긴다** → §4-2
- **프레임 0번 문제** — PixelLab 회전 애니메이션은 원본이 첫 장에 끼어 온다. 걷기에 그대로
  돌리면 한 장이 어긋난다. `_frames.gd`가 0번을 기준 프레임으로 따로 뺀다
- **소수점 자리는 도트를 죽인다.** 그리는 쪽(`_draw`)도 놓는 쪽(노드 위치)도 같다
- **화풍은 생성할 때가 아니라 만든 뒤에 잡는 것이 싸다.** 채도를 프롬프트로 조절하려 했으면
  헛돈을 썼다. `_desat.gd` 숫자 하나로 됐다 (`PIXELLAB.md` §3과 같은 결론)
- **번쩍이는 애니메이션보다 박자가 먼저다.** 전투가 심심했던 원인은 그림이 아니라
  글 읽을 틈 없이 넘어가는 박자였다. 프레임을 더 그리기 전에 **잡는 시간**부터 볼 것 → §1

---

## 8. 더 읽을 것

- **[Slynyrd Pixelblog 목록](https://www.slynyrd.com/pixelblog-catalogue)** — 도트 강좌의 표준.
  [애니메이션 입문](https://www.slynyrd.com/blog/2018/8/19/pixelblog-8-intro-to-animation) ·
  [근접 공격](https://www.slynyrd.com/blog/2018/9/8/pixelblog-9-melee-attacks)(스미어·잡는 프레임) ·
  [바람 이펙트](https://www.slynyrd.com/blog/2021/7/16/pixelblog-33-wind-effects)
- [12원칙의 도트 번역](https://www.sprite-ai.art/guides/animation-principles) — §2의 숫자들이 여기서 왔다
- [VFX Apprentice — 도트 이펙트](https://www.vfxapprentice.com/blog/pixel-art-game-effects-animated) —
  적은 프레임으로 이펙트를 만드는 실례. 고해상도로 그려 도트로 내려깎는 요령도
- [Lospec 강좌 모음](https://lospec.com/pixel-art-tutorials) — 디더링·클러스터·밴딩·팔레트 등
  500개. [팔레트 목록](https://lospec.com/palette-list)도 여기
- [Godot — Image](https://docs.godotengine.org/en/stable/classes/class_image.html) ·
  [ImageTexture](https://docs.godotengine.org/en/stable/classes/class_imagetexture.html)
- [Pixelorama](https://orama-interactive.itch.io/pixelorama) — 무료 도트 편집기. Godot로 만들어졌다
- [Aseprite Wizard](https://godotengine.org/asset-library/asset/713) — Aseprite → `SpriteFrames` 자동
- [pixel-sprite-generator](https://github.com/zfedoran/pixel-sprite-generator) — 절차적 스프라이트의 표본
- `PIXELLAB.md` — 생성으로 뽑는 쪽 · `EFFECT.md` — 움직이는 쪽 · `SOUND.md` — 소리 쪽
