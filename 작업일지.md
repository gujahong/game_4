# 작업일지

**이 파일을 먼저 읽을 것.** 컴퓨터 2대를 오가며 개발 중이라 대화 기록이 넘어오지 않는다.
"지금 어디까지 왔고, 최근에 뭘 왜 그렇게 정했고, 다음에 뭘 할지"만 짧게 담는다. 규칙과 환경
정보는 `CLAUDE.md`에 있다.

---

## 🕯️ 화풍을 정했다 (2026-08-11)

첫 세션. 발할라·커피톡 같은 **대화 기반 게임**을 만들기로 했고, 참고로 가져온 그림은 고딕·오컬트
분위기의 1비트 흑백 디더링 픽셀아트 넉 장이었다. 이번 세션에서 한 것은 **그 화풍을 실제로 뽑는
방법을 확정한 것**이고, 게임 자체는 아직 한 줄도 없다.

### 회원님이 정한 것

| | |
|---|---|
| 만들 곳 | 습작4 (우주쓰레기게임과 별개의 새 게임) |
| 화풍 | 1비트 흑백 디더링 — 단 완전 흑백이 아니라 **"색이 있는데 흑백 필터를 낀 느낌"** |
| 남길 색 | 그 그림에 필수인 색만. 피면 빨강, 숲이면 초록, 스테인드글라스면 여러 색 |
| 구조 | 거점 하나 + 장소 몇 곳 (아직 확정 아님) |
| 컨셉 | **주인공이 등불을 들고 다닌다** |

### 화풍을 만드는 방법

AI에게 "1비트로 그려줘"라고 시키지 않는다. **컬러로 뽑고 우리 필터를 씌운다.**

```
PixelLab로 컬러 배경을 뽑는다  →  실행 중에 DitherFilter를 씌운다  →  등불만 필터 밖에 얹는다
```

같은 필터를 통과시키면 배경이 몇 장이든 화풍이 저절로 통일된다. 그리고 **필터는 PNG를 바꾸지
않으므로** 나중에 값 하나만 고치면 전부가 따라온다. 이게 이 방식을 고른 이유다.

색은 **원본의 채도로 자동 판별**한다. "중요한 색"을 기계가 알 수는 없지만, 스테인드글라스·불·
이끼처럼 꼭 있어야 할 색은 원본에서도 대개 채도가 높다는 전제다. 예배당에서 실제로 잘 맞았다.

### 만든 것

| 파일 | 하는 일 |
|---|---|
| `shaders/DitherFilter.gdshader` | 계조 깎기 → 8x8 오더드 디더 → 틴트 → 채도 높은 곳만 색 복원 |
| `tools/FilterPreview.gd` / `.tscn` | 슬라이더로 그 값을 맞추는 도구. Space 배경 넘김 / L 등불 / Tab 패널 / C 값 복사 / S 저장 |
| `assets/backgrounds/chapel.png` | 버려진 고딕 예배당. pixen 640x360, 1 generation |
| `assets/backgrounds/forest.png` | 어두운 숲. 색이 많은 장면에서도 같은 값이 먹히는지 시험용 |
| `project.godot` | 640x360 내부 해상도, 정수배 확대, nearest 필터 |

확정된 값은 `CLAUDE.md`에 적어뒀다. D3D12에서 실제로 그려지는 것까지 확인했다.

### 겪은 것 두 가지

- **숲에서 초록이 거의 죽었다.** 나무의 초록이 원래 탁해서 채도 기준을 못 넘고, 살아남은
  색마저 탈색량 0.5에 절반이 된다. 회원님은 **그대로 좋다**고 하셨다 — "색이 남은 곳이 곧
  시선이 가는 곳"이라는 규칙으로 받아들이기로 했다
- **배경 안의 촛불도 회색이 된다**는 문제를 제기했으나(등불만 색이라는 규칙과 충돌),
  회원님이 **접자고 하셨다.** 배경은 다시 뽑지 않는다. 등불은 그냥 주인공이 드는 물건이다

### 시행착오 (다시 겪지 말 것)

- 처음에 "black and white monochrome"으로 프롬프트를 짰다가 뒤집었다. **원본에 색이 없으면
  필터가 걷어낼 색도 없다.** 컬러로 뽑아야 한다
- 탈색량을 1.0으로 올려 세상을 완전 회색으로 만들어봤으나 **0.5로 원복**했다

---

---

## 💬 대화 시스템을 옮겨왔다 (2026-08-11, 같은 세션)

우주쓰레기게임(`Documents/연습/우주쓰레기게임`)에 이미 있던 것을 골라 왔다. **복사해오고 두
프로젝트가 갈라지는 것을 받아들이기로 했다** — 공유 라이브러리로 묶으면 컴퓨터 두 대를 오가는
상황에서 폴더가 하나 더 늘어 오히려 짐이 된다.

### 가져온 것

| | |
|---|---|
| `scripts/dialogue/` | Controller(354줄) + 데이터 스키마 6개. **거의 그대로** |
| `scripts/dialogue/commands/` | wait, fade_in/out, flash, blackout, flicker, shake, set_auto_advance |
| `scripts/ScreenEffectLayer.gd` | 페이드/암전/플래시. 자체 완결이라 그대로 |

Controller의 진짜 값어치는 코드가 아니라 **주석에 남은 버그 기록**이다 — 재진입 방지가 왜 bool이
아니라 카운터인지, 짧은 줄이 빠른 두 번 클릭에 씹히던 것, 스킵이 첫 줄만 넘기고 멈추던 것,
연출이 긴 줄이 연출 끝나기 전에 넘어가던 것. 새로 짰으면 넷 다 다시 겪었다.

### 안 가져온 것 (필요해지면 그쪽에서 다시 꺼내면 된다)

- `zoom` — GameCamera가 있어야 한다. 이 게임엔 카메라가 없다
- 사운드 커맨드 4개 — `SfxManager`(448줄, 그 게임 효과음 파일이 박혀 있다)가 통째로 딸려온다
- `emergency_light` / `cargo_panel` — 우주선 비상등, 화물칸
- `DialogueUI` — **구조만 베끼고 새로 짰다.** 그쪽은 SF HUD 테두리와 CRT 켜짐 연출이 붙어 있어
  이 게임의 회색 고딕 화면과 어긋난다
- `DialogueEffectLayer`(CRT 스캔라인), `DialogueOpening`(512줄), `DialogueShowcase`(316줄)

### 새로 만든 것

| 파일 | 하는 일 |
|---|---|
| `scripts/DialogueUI.gd` | 대사창. 시그널만 구독하는 얕은 결합은 그대로, 겉모습은 새로 |
| `scripts/FilteredBackground.gd` | 배경에 DitherFilter를 씌워 띄운다. 배경은 이 노드로만 올린다 |
| `scripts/LampGlow.gd` | 등불 빛. **필터 바깥**에 있어야 색을 지킨다(자식으로 넣으면 회색이 된다) |
| `scripts/KoreanFont.gd` | Godot 기본 폰트에 한글이 없어서 시스템 폰트를 빌린다 |
| `scenes/DialogueTest.tscn` | 확인용. 타이핑/진행/style/커맨드/선택지가 도는지만 본다 |

메인 씬은 `scenes/DialogueTest.tscn`이다. 실제로 D3D12에서 돌려 화면까지 확인했다.

### 알아둘 것

- 두 프로젝트의 내부 해상도가 **똑같이 640x360**이라 레이아웃이 그대로 맞았다
- 다만 확대 방식이 다르다. 그쪽은 `canvas_items`(글자만 고해상도로 또렷), 이쪽은
  `viewport`(전부 640x360에서 그린 뒤 확대). 이쪽이 픽셀아트엔 맞지만 **글자가 도트로 뭉툭해진다**
- **한글 픽셀 폰트가 없다.** 지금은 맑은 고딕(안티앨리어싱된 벡터 폰트)을 빌려 쓰는 임시 상태라
  글자만 화풍에서 겉돈다. `KoreanFont.gd`만 갈아끼우면 되게 해뒀다

---

## 다음에 할 일

### 🗡️ 게임의 방향이 정해졌다 (2026-08-11)

처음엔 발할라·커피톡 같은 **대화 기반**으로 잡았는데, 이야기하는 과정에서 회원님이 방향을
바꾸셨다. 아래가 회원님이 직접 정하신 것이다.

| | |
|---|---|
| 톤 | **다크 판타지** |
| 주인공 | **판타지 세계를 떠도는 방랑 모험가** — 가게 주인이 아니라 길 위의 사람 |
| 전투 | **있다. 피어 앤 헝거(Fear & Hunger) 같은 느낌** |

**"손님이 나에게 온다"가 "내가 찾아간다"로 뒤집혔다.** 그래서 등불이 오히려 자연스러워졌다 —
가게 주인이 등불을 드는 건 어색했지만 밤길을 걷는 방랑자가 드는 건 당연하다.

**아직 안 정한 것: 세계관, 왜 떠도는가, 전투 세부.** 그리고 **게임의 무게중심이 전투인지 대화인지**도
안 정했다 — 이건 말로 정하지 말고 전투 한 판 만들어 돌려보고 정하기로 했다.

이 결정의 대가를 적어둔다. 피어 앤 헝거는 대화 게임이 아니라 던전 생존 게임이다. 그 전투를
가져오면 **지금까지 만든 대사 시스템은 주인공이 아니라 조연이 된다.** 알고 가는 것이다.

### 🌌 세계관이 정해졌다 (2026-08-11) — 회원님이 정하신 것

```
이 세상들은 어떤 초월적인 무언가의 꿈이 세상이 된 것이다.
그 꿈들에 이상이 생긴다.
주인공은 그 꿈으로 건너가 그곳의 문제를 푼다.
```

| | |
|---|---|
| 주인공 | **세계를 지키는 비밀 결사의 수호자** |
| 거점 | **아카식 레코드처럼 보이는 곳** — 모든 것이 기록된 서고 |
| 결사의 목표 | **맥거핀. 밝히지 않는다** |
| 장소마다 노릴 것 | **순례 · 경외 · 공포 · 신비로움** |

**결사의 목표를 안 밝히는 것이 방침이다.** 이 게임은 이기는 게 아니라 세계관을 즐기는
게임이라, 답을 주면 오히려 재미가 준다.

### 게임 시작에 띄울 문장

> **세계를 건너다니는 자에게는 그곳이 온전히 보이지 않는다.**
> **등불이 비추는 것만 실재한다.**

회원님이 고르신 문장이다. 이게 규칙 셋을 한꺼번에 설명한다 — 화면이 왜 회색인지, 색을 가진
것이 왜 등불뿐인지, 왜 기름을 태워야 볼 수 있는지. **설정과 시스템이 같은 문장으로 묶인다.**

### 이 세계관이 잠근 것들

- 참고그림 열두 장이 화풍도 색도 제각각인 것 → **꿈마다 다른 세계니까 당연하다.** 밝은 SF도
  어두운 고딕도 전부 쓸 수 있다
- 사진 더미가 왜 있는가 → **꿈은 잊히니까 기록한다.** 그리고 거점이 아카식 레코드(기록의 서고)
  라서, 사진 더미가 UI가 아니라 **결사의 물건**이 된다
- 필터가 왜 모든 세계를 회색으로 만드는가 → 위 문장 그대로. 기술적 편의가 아니라 설정이다

### 아직 빈 곳

- 결사는 **무엇으로부터** 세계를 지키나 (꿈의 "이상"이 무엇인지)
- 등불은 무엇인가 — 결사의 물건인지, 세계를 건너는 수단인지
- 왜 수호자는 혼자인가 (사람을 안 그리기로 했으므로 다른 단원이 화면에 안 나온다)

셋 다 첫 장소의 글을 쓸 때 필요해진다. 그전까지는 미뤄도 된다.

### 🖼️ 방향을 다시 잡았다 (2026-08-11, 세션 끝 무렵)

**"아트를 즐기는 게임."** 옛날 포인트 앤 클릭 어드벤처 느낌으로 간다. 회원님이 정하셨다.

```
사진 더미   여정을 보여주는 화면. 지나온 곳이 좌우로 쌓인다
장소 하나   사진 한 장. 들어가면 화면을 가득 채운다
그 안에서   둘러보고, 클릭하고, 읽는다  ← 게임의 대부분
가끔        조우가 벌어진다 (만들어둔 전투 화면)
```

**이기는 게 아니라 보는 게 즐거움이다.** 그래서 전투는 주인공에서 내려오고, **대사 시스템이
다시 주인공 자리로 돌아온다** — 클릭해서 뜨는 게 결국 글이니까. 앞서 "피어 앤 헝거로 가면
대사 시스템이 조연이 된다"고 적어둔 걱정이 뒤집혔다.

만들 게 적은 것이 이 방향의 진짜 이점이다. **장소 하나 = 사진 한 장 + 클릭할 자리 몇 개 +
각각에 딸린 글.** 배경 1 generation, 글은 공짜다.

**아직 안 정한 것: 등불을 커서로 쓸 것인가.** 화면이 캄캄하고 마우스가 닿은 곳만 빛이 들어
보이는 방식이다. 그림을 한 번에 다 못 보게 만들어 천천히 보게 하는 장치이고, 지금까지 세운
규칙 셋(*안 보이는 것이 무섭다* / *색을 가진 건 등불뿐* / *기름을 태워야 알 수 있다*)이 전부
같은 동작으로 모인다. 다만 제안 단계다.

### 🗂️ 사진 더미 (2026-08-11)

`scripts/PhotoStack.gd`, 확인용 씬은 `scenes/PhotoTest.tscn`(Space로 한 장씩 얹힌다).

**pixen이 가로로 긴 캔버스에서 그림을 좌우로 갈라 내놓는 것이 계속 결함이었는데, 사진이
정사각형이면 그게 형식이 된다.** 게다가 **갈라진 이음매에서 자르면 배경 한 장이 사진 두 장이
된다** — 회원님 아이디어다. 가운데를 자르는 게 아니라 대칭인 자리를 자른다.

배경 4장을 잘라 사진 8장이 나왔다(`assets/photos/`, `tools/_split.gd`). **generation 0.**
그리고 자른 것이 원본보다 낫다 — `tower_gate_2`는 탑 하나에 붉은 문, 위가 잘린 구도인데,
프롬프트로 아무리 요청해도 안 되던 그림이 자르니까 그냥 나왔다.

사진 한 장은 **320x320**이다. 앞으로 새로 뽑는 것도 이 크기로 맞춘다.

**회전은 쓰지 않는다.** 폴라로이드처럼 비스듬히 겹치면 보기 좋겠지만 픽셀아트를 각도로 돌리면
도트 격자가 깨진다. 평행 이동 + 테두리 한 줄 + 밝기 차로만 층을 나눈다.

### 떠오른 안 (확정 아님)

**"수많은 초월적인 곳을 다니는 누군가"** — 회원님이 던지신 것이고, 지금까지 어긋나던 게 여기서
한꺼번에 맞는다.

- 참고그림 열두 장이 화풍도 색도 제각각인 게 **문제가 아니라 답이 된다.** 열두 장소인 셈
- **필터가 그것들을 묶는 장치가 된다.** 서로 상관없는 세계들이 한 게임이 되는 이유가
  "전부 같은 회색을 통과한다"는 것. 화풍 통일이 기술적 편의가 아니라 **설정**이 된다
- **등불이 유일한 상수다.** 장소는 매번 바뀌는데 내가 든 불빛만 안 바뀐다
- 장소마다 필터 틴트를 다르게 줘도 된다 — 세계가 다르니 갈라지는 게 맞다
- 만들기도 좋다. **장소 하나 = 배경 하나 + 조우 하나**, 서로 안 얽혀서 하나씩 늘리면 된다.
  다섯 곳이면 게임, 스무 곳이면 큰 게임. 중간에 멈춰도 완성품이다

### 만들 것

**여정의 고리에서 새로 만들 건 둘뿐이다.** 장소 이동은 `DialogueChoice.next_scene`이 이미 하고
있어서 지도나 이동 시스템이 필요 없다 — **선택지가 곧 길이다.**

1. 배경을 바꾸는 대사 커맨드 (열 줄쯤)
2. **전투 화면** — 1인칭 턴제, 적 그림 한 장 + 아래 메뉴. 부위별로 노린다

적 그림은 **전투 설계를 확정한 뒤에** 뽑기로 했다(회원님 요청). 아직 안 뽑았다.

**초상화.** 우주쓰레기게임의 `DialoguePortrait`(184x400 흉상) 구조는 쓸 만하다. 다만 이 게임의
인물 크기를 먼저 정해야 하고, 그건 "누가 나오는가"를 알아야 정할 수 있다.

---

## 🔤 글꼴을 정했다 (2026-08-11, 같은 세션)

**Neo둥근모 16px.** 갈무리11(12px)·갈무리9(10px)와 같은 화면에 넣어 비교했고, 회원님이 둥근모를
고르셨다. 셋 다 SIL Open Font License 1.1이라 상업적 이용·게임 임베드에 문제가 없다.

`assets/fonts/`에 `Galmuri11.ttf`(5.4MB)와 `Galmuri9.ttf`(4.6MB)가 아직 남아 있다 — 안 쓸 거면
지워도 된다.

### 겪은 것 (다시 겪지 말 것)

- **갈무리 비트맵판은 함정이다.** `Galmuri11Bitmap.ttf`는 내장 비트맵이 16px에만, `9Bitmap`은
  12px에만 들어 있다. 다른 크기에서는 글리프 높이가 0이라 **글자가 통째로 안 그려진다.**
  파일이 1/7로 작아서 솔깃하지만 쓰지 말 것. 외곽선판은 어느 크기에서도 정상이다
- `disable_embedded_bitmaps`를 굳이 끄지 말 것. 기본값(끄지 않음 = 외곽선 사용)이 맞다
- **기울임(`[i]`)을 픽셀 글꼴에 걸면 안 된다.** 도트를 비스듬히 밀어서 격자가 깨진다.
  내레이션 스타일에서 뺐고, 지금은 색만 다르게 간다
- 임포트 설정은 **글꼴 파일마다** 꺼야 한다. project.godot의 `[gui]`는 기본 글꼴에만 걸린다

(선택지 버튼 스타일은 같은 세션에 끝냈다 — 대사창과 같은 톤으로 가라앉혀두고, 지금 고르고 있는
것만 테두리와 글자가 등불색으로 살아난다. 첫 버튼에 포커스를 줘서 위/아래 + 스페이스로도 고를 수
있다.)

아직 안 정한 것: **왜 이 세계가 이런가, 주인공은 누구인가, 찾아오는 이들은 왜 오는가.**
이건 회원님이 정할 몫이고, 정해져야 손님도 대사도 나온다. 컨셉으로 정해진 것은
**"주인공이 등불을 들고 다닌다"** 하나뿐이다.

---

## ⚔️ 전투를 만들었다 (2026-08-11, 같은 세션)

**처음으로 눌러볼 수 있는 것이 생겼다.** 메인 씬이 `scenes/Battle.tscn`이다.

| 파일 | 하는 일 |
|---|---|
| `scripts/battle/Battle.gd` | 전투 진행. **화면을 모른다** — 시그널만 쏜다 |
| `scripts/battle/Lantern.gd` | 등불. 밝기 3단계의 수치표가 전부 여기 있다 |
| `scripts/battle/EnemyDef.gd` | 적 정의(Resource). **새 적 = 리소스 하나**, 코드 무수정 |
| `scripts/battle/BattleScreen.gd` | 화면. 밝기를 셰이더로 흘려보낸다 |
| `resources/hollow_armour.tres` | 속 빈 갑옷 |
| `scripts/UiStyle.gd` | 대사창과 전투 화면이 같은 톤을 쓰게 모아둔 것 |

로직과 화면을 나눈 것은 대사 시스템(`DialogueController` / `DialogueUI`)과 같은 방식이다.

**말을 걸어 푸는 길에는 기름값이 든다.** 속 빈 갑옷은 말을 세 번 건 뒤 **등불이 환할 때만**
물러간다. 어두우면 "아무것도 보이지 않는 듯하다"가 뜨고 안 통한다. 설득이 공짜가 아니게
만드는 장치다.

### 알아낸 것

- **탈색만으로는 어림도 없다.** 처음엔 밝기 단계에 `desaturate`만 물렸는데, 색만 빠지고 화면이
  안 어두워져서 불을 꺼도 적이 또렷했다. `layer_modulate`로 **실제 밝기까지 같이 내려야**
  세 단계가 갈린다(`Lantern.BRIGHTNESS`)
- 셰이더가 `COLOR`를 통째로 덮어써서 **노드의 `modulate`가 조용히 무시된다.** 아무 일도
  안 일어나서 알아채기 어렵다. `layer_modulate` uniform을 쓸 것
- 적 체력은 **숫자로 안 보여준다.** "멀쩡하다 / 금이 갔다 / 무너지기 직전이다"로만.
  눈금이 보이면 계산이 되고, 계산이 되면 무섭지 않다

### 등불은 5단계다

꺼짐 / 불씨 / 어스름 / 밝음 / 환함. 수치표는 전부 `Lantern.gd` 맨 위에 모여 있다.

**세상과 불빛이 반대로 움직인다**(회원님 요청). 단계가 내려갈수록 세상은 어두워지고 색이
빠지는데(`BRIGHTNESS`, `DESATURATE`), **불빛 자체는 더 도드라진다**(`LIGHT_INTENSITY`).
캄캄할수록 작은 불빛이 강하게 보이는 게 실제로 맞고, 화면에서도 그래야 "이 불이 내가 가진
전부"로 읽힌다. **맨 아래 단계는 밝기 0 — 진짜 암흑이라 배경도 적도 사라지고 UI만 남는다.**

돌려보니 **어두운 쪽이 훨씬 낫다.** 불씨 단계에서 세상이 거의 죽고 빛줄기만 살아남는 화면이
이 게임의 그림이다.

### 메뉴가 등불에서 나온 빛이다

`scripts/battle/LightMenu.gd`. 상자 버튼을 늘어놓지 않고, 화면 아래 가운데(내가 등불을 든
자리)에서 빛줄기가 뻗어 나가고 그 끝에 **공격 / 방어 / 대화 / 도주**가 붙는다. 회원님이
스케치로 주신 구조다.

상자를 안 쓰는 이유는 이 게임 규칙 때문이다 — 화면에서 색을 가진 것은 등불뿐이라 UI에 회색
상자를 늘어놓으면 규칙이 깨진다. **메뉴가 빛이면 UI가 설정의 일부가 되고**, 등불이 약해질 때
메뉴도 같이 흐려진다. 단 글자만은 최소 밝기를 지킨다(`MIN_TEXT_ALPHA`) — 안 보여야 하는 것은
**적**이지 내 손이 아니다.

### CRT는 아직 확정 아니다

`shaders/CrtScreen.gdshader` + `scripts/CrtOverlay.gd`. 볼록렌즈 왜곡 + 가로 주사선 +
가장자리 어둡게. **C 키로 껐다 켰다** 하며 비교하라고 만들어둔 것이고, 쓸지 말지는 안 정했다.

대가가 하나 있다. 이 게임은 640x360을 정수배로만 확대해 도트가 화면 픽셀에 딱 떨어지는 것이
규칙인데 **왜곡이 그 규칙을 깬다.** 가장자리로 갈수록 도트가 늘어진다. 진짜 CRT가 그랬다고
볼 수도, 애써 지킨 격자를 스스로 무너뜨리는 것이라고 볼 수도 있다.

**함정 하나**: CRT가 켜진 채로 `get_viewport().get_texture().get_image()`를 부르면 힙 손상으로
즉시 죽는다. 화면 텍스처를 다시 읽는 셰이더와 스크린샷이 부딪힌다. 프레임을 두어 번 더
기다리면 피해진다(`BattleScreen._shoot`).

### 아직 안 해본 것

**밸런스를 한 번도 안 돌려봤다.** 수치는 전부 첫 짐작이다 — 특히 큰 공격 15가 체력 40에서
충분히 아픈지가 관건이다. 안 아프면 어둠 속에서 뭉개는 게 정답이 되고, 그러면 등불 선택이
성립하지 않는다. 수치는 전부 `Lantern.gd`와 `Battle.gd` 맨 위에 모여 있다.

---

---

## 📐 해상도를 960x540으로 옮겼다 (2026-08-11)

**640x360에서 올렸다.** 이유는 하나다 — **512x512 사진이 세로로 들어가야 하는데 360에는 안
들어간다.** 그리고 1080p에 정확히 2배, 4K에 4배로 떨어지는 것은 960x540뿐이다(1280x720은
1080p에서 1.5배라 도트가 깨진다).

앞서 "해상도를 올려도 이득이 없다"고 적어둔 것은 **배경 한 장이 화면 전체를 덮던 때** 얘기다.
화면을 정사각형 사진 여러 장으로 채우는 구조가 되면서 전제가 바뀌었다.

**크기와 상관없이 전부 1 generation**이라, 320x320(102,400픽셀) 대신 512x512(262,144픽셀)를
뽑으면 같은 값에 2.5배다. 작게 뽑는 것은 그냥 손해다.

512x512가 갈라지지 않는 것은 실제로 뽑아 확인했다(`assets/photos/obelisk.png`). 옆을 안개와
바위로 채우라고 지시한 것이 먹혔다 — **빈자리를 안 채워주면 모델이 주제를 복제해서 채운다.**

옮기면서 손댄 것: `project.godot`, `DialogueUI`, `BattleScreen`, `LightMenu`, `PhotoStack`,
`Lantern.GLOW_SIZE`, `FilterPreview`, `FilteredBackground`(늘리지 않고 가운데 놓게), 씬 두 개.
좌표가 전부 각 파일 맨 위 상수에 모여 있어서 구조는 안 건드렸다.

**화면을 통째로 덮는 그림은 이제 못 만든다** — 960x540은 pixen의 면적 상한을 넘는다. 그래서
`FilteredBackground`는 늘리지 않고 가운데 놓는다.

---

## 🗺️ 맵 RPG로 틀었다 (2026-08-11, 세션 끝)

**포인트 앤 클릭 대신 피어 앤 헝거처럼 걸어다니는 맵 게임으로 간다.** 회원님이 정하셨다.
일러스트는 잠시 접고 맵부터 만든다.

### 왜 이게 말이 되는가

**오늘 내내 붙들고 있던 벽(960x540짜리 그림을 못 뽑는다)이 통째로 사라진다.**

```
맵       타일을 반복해서 깐다     → 면적 상한과 무관
일러스트  조우·이벤트 때만 띄운다  → 화면을 안 채워도 된다
```

그리고 **맵 쪽은 화풍이 도구로 잡힌다.** 일러스트에서 계속 실패하던 부분이다.

- 타일셋끼리: `lower_base_tile_id`로 앞 타일셋의 타일을 물려받아 이어진다
- 소품: `create_map_object`에 **맵 화면 조각(192x192 이내)** 을 `background_image`로 넘기면
  그 화풍에 맞춰 그려준다. 인페인팅이라 배경이 복제될 일도 없다

**비용도 내가 과장했던 것으로 드러났다.** 문서 확인 결과:

```
탑다운 타일셋 32px    1 gen   (64px는 pro라 20~40)
소품 하나             1 gen
캐릭터 걷기           1 gen / 방향  ← 4방향이면 4 gen
```

기지 하나 만드는 데 대략 10~12 generations다.

### 일 분담 (공식 안내 `pixellab://docs/godot/wang-tilesets`)

```
내가        타일셋 생성 → 내려받기 → 변환 스크립트 → Godot TileSet(.tres) + 씬까지
회원님이    씬 열고 TileMap 탭 → Terrains 탭 → 지형 고르고 → R키로 사각형 드래그
```

**반드시 R(사각형 도구)를 쓴다.** D(붓)는 코너 기반 지형에서 작동하지 않는다고 안내에 못박혀 있다.

### 공식 영상을 보고 설정을 고쳤다

회원님이 [Tilesets 튜토리얼](https://www.youtube.com/watch?v=sE1s5ZYfxqk)과
[high top-down 튜토리얼](https://www.youtube.com/watch?v=q9z2Vhpz-Z8) 자막을 가져다주셨다.
**내가 잡은 값이 세 군데 틀렸다.** 자세한 것은 `CLAUDE.md`의 "타일셋" 절에 정리했다.

```
transition_size   0.25  →  0.5      0.5가 "대부분의 게임에서 보는 표준 벽"
detail            highly → low       32px엔 highly detailed가 안 들어간다
outline           lineless → 켠다    작은 타일은 외곽선이 형태를 잡아준다
묘사              길게 → 두 단어      "marble floor" / "starry void"
```

그리고 **64px은 다른 도구**였다. `high top-down` 타일셋은 16/32뿐이고, 32/48/64/96은 tiles 탭의
새 베타 도구다. 내가 둘을 섞어서 "64 pro로 가자"고 했는데 20~40 gen을 헛되이 태울 뻔했다.

**Godot 경로도 훨씬 쉽다.** MCP 자료엔 변환 스크립트를 쓰라고 나오지만, 영상은 웹에서
"export as tileset 15"로 내보내 **TileMapDual 플러그인**에 드래그하면 끝이라고 보여준다. 코드 0줄.

### 첫 타일셋 — 아카식 기지 바닥

```
공허(별하늘) ↔ 대리석 바닥 · 32px · transition_size 0.25 · lineless · highly detailed
tileset id  b4fc6c67-7a03-410a-a9a5-47cb8c0d0dfb
base tile   공허   013bca70-16c8-41d3-8ffd-b214e93531d4
            대리석 97835612-1928-4c19-a0eb-c28755fecf99
```

**base tile ID는 버리지 말 것.** 다음 타일셋(대리석↔카펫 등)을 이을 때 이게 있어야 같은
대리석이 이어진다.

`transition_size`는 0.25로 잡았다 — 0이면 전환 묘사가 안 먹고, 0.5를 넘으면 타일이 25장이 되어
Godot 변환기가 다른 방식을 써야 한다(16장이어야 코너 기반 지형이 그대로 된다).

### ⚔️ 전투 컨셉이 정해졌다 — 랜턴에서 무기를 뽑는다

회원님이 정하셨다.

```
랜턴에서 무기를 뽑아 쓴다  →  뽑을수록 랜턴이 어두워진다  →  앞이 안 보인다
```

**싸울수록 시야를 잃는다.** 탑다운 맵에서 랜턴은 시야 반경이므로, 무기를 뽑을수록 화면이
좁아진다. 기름 같은 별도 자원이 필요 없다 — **빛 자체가 탄약**이다.

이미 만든 `Lantern.gd`가 그대로 쓰인다. 밝기 5단계와 단계별 시야·명중·화면 밝기가 이미 있으니,
**"기름을 태운다"를 "무기를 뽑는다"로 바꾸기만** 하면 된다.

### 접어둔 것

일러스트 쪽에서 확인한 것들은 남겨둔다 — 나중에 조우/이벤트 그림을 만들 때 그대로 쓴다.

- **512x512가 자유 그림의 천장**이다(면적 상한 262,144). 어떤 변환 경로로도 못 넘는다
- **시드와 파라미터를 고정해도 화풍이 안 잡힌다.** 성당과 피라미드를 같은 설정(seed 1000,
  lineless, highly detailed)으로 뽑았는데 딴판이었다. **소재가 화풍을 끌고 간다**
- 필터는 **팔레트·명암·채도는 잡지만 선 굵기와 묘사 밀도는 못 잡는다.** 회원님 표현으로
  "엄청 다른 게임 같아"

---

## 📖 PixelLab 설명서를 따로 썼다 — `PIXELLAB.md`

공식 영상 일곱 편과 문서를 정리했다. **출처에 우선순위를 박아뒀다** — 영상 > 공식 문서 >
우리 경험. 우리가 겪은 것은 §10에 따로 모았고, 위와 충돌하면 위가 맞다.

이번 세션에 내 짐작이 여러 번 틀렸기 때문이다. 대표적으로:

- "512가 그림 크기의 상한이다" → **아니다.** 웹에서 캔버스를 늘려 조각씩 이어 그릴 수 있다
- "64px 타일은 pro로 20~40 gen" → **아니다.** 도구를 둘 섞어 말했다. high top-down은 16/32뿐
- "화풍은 시드와 파라미터로 잡는다" → **아니다.** 만든 뒤 **팔레트로 강제**하는 것이 공식 답이다
- "맵 RPG는 그림 값이 많이 든다" → **아니다.** V3 애니메이션은 1~8 gen이다

---

---

## 🕯️ 조우 연출을 만들었고, 그게 이 게임의 얼굴이 됐다 (2026-08-11, 다른 컴퓨터)

**처음으로 "이거다" 소리가 나온 화면이다.** 회원님 말씀 그대로 옮기면 *"느낌 개좋은데? 내가
고민하던 게 이걸로 해결된 것 같아."*

| 파일 | |
|---|---|
| `scenes/Walk.tscn` / `scripts/walk/WalkScene.gd` | 조우 연출. 새로 만든 것은 이 둘뿐 |

### 무엇인가

일러스트 한 장이 화면을 채우고, 그림 속 제단에 **그것**이 서 있고, 내가 아래에서 걸어
들어간다. 닿으면 전투가 시작된다.

```
↑↓←→  걷기(끝까지 12초)   [ ]  그것의 발 높이   - =  그것의 크기
Space 다음 사진            D 달리 인   F 채우기   Q 단계로 끊기   G 격자 기준   R 처음부터
```

### 왜 통했는가 (이게 제일 중요하다)

1. **그림이 원근을 이미 그려놨다.** 코드가 하는 일은 점 하나를 위로 밀고 크기를 줄이는 것뿐이다.
   3D도 시차 레이어도 없다
2. **화면을 채운 것.** 320짜리 사진이 검은 화면 가운데 떠 있을 땐 "사진을 보는" 느낌이었는데,
   높이에 맞춰 늘리니 "여기 서 있는" 게 됐다. 이 한 끗이 컸다
3. **느린 속도.** 12초 동안 아무 일도 안 일어나는 것이 외로움을 만든다

### 시점이 정해졌다 — 1점 투시 정면, 카메라는 내 뒤

회원님이 주신 참고 그림(협곡·별밤 수도승·대리석 계단·눈알 피라미드 등)이 **예외 없이 1점
투시**였고, 사람은 전부 **등을 보인 채** 안쪽을 향해 있었다. 등이 보인다는 건 카메라가 옆이
아니라 **내 뒤**에 있다는 뜻이다.

**이래서 옆보기(2D 횡스크롤)를 버렸다.** 옆보기는 깊이 축이 없어서 저 그림들의 힘이 안 나온다.

그리고 **전투 화면이 이미 같은 시점이었다.** `BattleScreen`은 적이 위 가운데(480,176),
등불이 아래 가운데(480,444)다. 걷기와 전투가 카메라를 공유하므로 조우할 때 시점을 안 돌려도 된다.

### 게임 구조가 정해졌다

```
지역 탐색   탑다운   ← 평소
조우/보스   이 연출  ← 어쩌다 한 번
전투        1인칭 턴제 (이미 만들어둔 것)
```

**이 연출을 보스에만 쓰기로 한 것은 회원님 결정이다.** 매 장면이 이러면 무뎌지지만 어쩌다
한 번이면 사건이 된다 — **압도감은 희소해야 산다.**

탑다운이 돌아오면서 타일셋·8방향 캐릭터 비용도 같이 돌아왔다. 다만 **화풍 걱정은 줄었다** —
드퀘도 피어 앤 헝거도 필드와 조우 화면이 딴판이고 아무도 이상하게 안 본다. 시점이 다르면
다른 그림인 게 자연스럽다.

### 겪은 것 세 가지 (다시 겪지 말 것)

- **실루엣이 등불에 지워졌다.** 등불 빛이 `blend_add`라서 검은 사람보다 나중에 그려지면 빛이
  더해져 사람이 사라진다. 첫 캡처에서 사람이 아예 안 보였다. **실루엣을 등불보다 위에** 그린다.
  참고 그림의 협곡도 밝은 빛 **안에** 검은 점이 박힌 구조다
- **디더 격자가 그물처럼 기어다녔다.** 배율이 1.00 → 1.01 → 1.02로 조금씩 변할 때마다 격자가
  다시 짜인다. → **격자를 화면에 못 박아 해결했다**(아래 참고)
- **그것이 배경 위를 미끄러졌다.** 그것한테 배경과 다른 원근 곡선(깊이^2.2)을 줬더니 제단에
  서 있는 게 아니라 따로 놀았다(회원님 지적). → **그것의 자리와 크기를 그림 사각형에 대한
  비율로만** 정한다. 이제 깊이를 아예 안 본다

### 그것은 안 움직이고, 내가 작아진다

**경외감은 그것이 커지는 데서 오는 게 아니라 내가 작아지는 데서 온다.** 협곡 그림이 그렇게 되어
있다. 그래서 내 키는 그림 높이의 16% → 4.5%로 줄고, 그것은 처음부터 끝까지 그 자리에 그대로 크다.

우리가 싸울 것들은 대개 **엄청나게 커다란 무언가**라서(회원님) 이게 성립한다.

그리고 안 움직이는 게 더 무섭다 — 쫓아오면 도망이라도 치는데, 기다리고 있으면 내 발로 가는
수밖에 없다.

### 디더 격자를 화면에 못 박기로 확정했다

`DitherFilter`에 `dither_on_screen` uniform을 넣고 **기본값 true**로 뒀다. 자세한 것은
`CLAUDE.md`의 "고정된 값"에 적어뒀다. 게임 전체에 걸리지만 다른 것들은 전부 1배로 놓여 있어서
보기에 달라지는 것은 없다.

컨셉에도 이쪽이 맞다 — *"색이 있는데 흑백 필터를 낀 느낌"*이라면 필터는 화면에 얹히는 것이지
그림에 붙어 있는 것이 아니다.

### 확인한 것: 적 그림은 다시 안 뽑아도 된다

`hollow_armour`가 "녹슨 주황이 등불과 경쟁하고 게임 에셋처럼 보인다"고 걱정했는데, **필터를
통과시키고 성당 안에 세우니 괜찮다.** 주황이 대부분 빠져 잿빛 강철로 읽히고, 배경과 같은
필터라 오려붙인 티도 안 난다.

---

## 🎨 그림을 실제로 열어보고 알아낸 것 (2026-08-11)

**그동안 문서만 읽고 그림 이야기를 했는데, 이번에 열한 장을 다 열어봤다.**

### 자른 것이 방법이었다

여덟 장이 여덟 장 다 산다. `chapel_1`·`chapel_2`가 제일 좋고(안개·빛줄기·안쪽으로 물러나는
공간), `forest_1`·`forest_2`도 오솔길이 안개로 사라지는 구도가 됐다. `tower_gate_2`는 압도감
규칙 넷 중 셋이 그냥 맞아떨어져 있다.

### 화풍이 세 집안으로 갈린다

```
부드러운 쪽   chapel_1/2, forest_1/2, obelisk    외곽선 없음, 안개로 윤곽이 풀림  ← 목표
외곽선 쪽     tower_1/2, tower_gate_1/2          검은 선이 굵고 색면이 평평함
혼자          pyramid                            얼굴 달린 피라미드, 다른 장르
```

**`obelisk`가 목표에 제일 가깝다.** 회색 바위와 안개인데 갈라진 틈만 붉게 살아 있다 —
"쨍한 것만 색이 남는다"가 필터를 씌우기 전에 원본에서 이미 그렇게 그려져 있다.

### ★ 화풍은 파라미터가 아니라 소재가 끈다

`PIXELLAB.md` §3은 "만든 뒤 팔레트로 강제하라"가 답이라고 하는데, **우리 경우엔 그것도 아니다.**
성당과 피라미드를 같은 설정(seed 1000, lineless, highly detailed)으로 뽑았는데 딴판이었다.

```
회화로 끌리는 것   안개 · 바위 · 구름 · 물 · 빛 · 먼지 · 사막 · 눈보라
만화로 끌리는 것   성당 · 첨탑 · 철책 · 상징물 · 정면을 보는 얼굴
```

픽셀아트에서 고딕 성당은 이미 캐슬바니아고, 눈 달린 피라미드는 이미 만화다. 모델이 그렇게
배웠다. **그러니 "회화처럼"은 형용사가 아니라 무엇을 그리느냐로 정한다.**

그리고 팔레트가 우리한테 덜 중요한 이유가 하나 더 있다 — **화면 필터가 어차피 4계조로 다시
깎는다.** 필터가 팔레트 일을 이미 하고 있다. 못 잡는 것은 **선 굵기와 묘사 밀도**뿐이고,
그건 팔레트로 안 잡힌다.

### `CLAUDE.md`의 "화풍을 붙잡는 법" 절은 낡았다

거기 적힌 "① 모델 고정 ② 시드 고정 ③ 파라미터 고정 ④ 어휘 고정 — 통했던 방법은 이 넷뿐"은
`PIXELLAB.md` §10과 위 관찰로 뒤집혔다. **④(어휘)만 유효하고 ②③은 안 통한다.**

---

## 🐛 아직 안 고친 것

- **`DitherFilter`의 `input_black` / `input_white`가 죽어 있다.** uniform도 있고
  `FilterPreview`에 "흑점/백점" 슬라이더도 붙어 있는데 `fragment()`에서 안 쓴다. 돌려도
  아무 일이 안 일어난다. 주석에 의도는 길게 적혀 있으니 구현만 하면 된다
- **사진 크기가 320·448·512로 섞여 있다.** `PhotoStack.PHOTO_SIZE`는 448로 테두리를 그리는데
  실제 사진 대부분은 320이다
- `chapel448.png`가 하얗게 날아가 있다. 정체 불명(실패한 생성이거나 미리보기 캡처)
- `assets/fonts/Galmuri11.ttf`·`Galmuri9.ttf` 10MB가 안 쓰이고 남아 있다
- `assets/tilesets/void_marble_*`은 `marble_void`의 위아래를 뒤집은 짝인데 아무도 안 쓴다

---

## 다음 세션에 할 일

**앞선 "팔레트 → 주인공 8방향 → 걷기 애니메이션" 계획은 접었다.** 조우 연출로 방향이 바뀌면서
8방향 캐릭터가 당장 필요 없어졌고, 팔레트는 화면 필터가 대신하고 있다.

1. **메인화면 그림을 뽑는다.** 아래 승인 대기 중인 프롬프트가 있다
2. **조우 → 전투 전환.** 회원님이 정하신 순서는 **컷 암전 → 등불 스르르 → 전투 씬**이다.
   `BattleScreen`도 같이 손봐야 한다(캄캄하게 시작해서 등불이 켜지며 적이 드러나야 한다)
3. **그림마다 그것의 발 높이·크기를 맞춰서 데이터로 적어둔다.** `Walk.tscn`에서 `[ ] - =`로
   맞추면 화면에 값이 나온다
4. 실루엣을 제대로 된 뒷모습 그림으로 (지금은 코드로 그린 머리+몸통)

### 메인화면 — 승인 대기 중

거점은 **거대한 금색 고리가 안쪽 빛으로 감겨 들어가는 곳**이다(회원님이 참고 그림 넉 장을
주셨다: 우주 태피스트리 / 나선 코덱스 / 고리 세계 / 검은 태양). 넷의 공통점은
**검은 바탕 + 금색 하나 + 동심원 구조 + 아래쪽 지형**이다.

주의: 그중 **나선 코덱스와 우주 태피스트리는 밀도 자체가 그림**이라 512에 4계조로 깎으면
얼룩이 된다. **고리 세계와 검은 태양** 쪽 구조로 간다.

```
create_image_pixen · 512x512 · lineless · highly detailed · detailed shading · 1 gen

a vast black void, an enormous ring of pale gold light curving away toward a
single bright core far in the distance, concentric bands of tiny glowing
points along the ring, thick banks of dim cloud beneath it, dark jagged
mountain ridges across the bottom edge, deep haze, gold and grey only
```

- `symmetrical`은 안 쓴다(좌우 거울 복제를 부른다). `curving away toward a single core`로 대신한다
- 아래 구름·산등성이는 **빈자리를 안 채워주면 모델이 주제를 복제하기 때문**이고, 동시에
  크기를 알려주는 비교 대상이 된다
- 640x360이 아니라 512 정사각형인 이유: 가로로 길면 좌우로 갈라지는데, **가운데 하나가 중심인
  구도에서는 그게 바로 실패**다. 그리고 배경이 검은 공허라 화면 좌우 여백이 안 보인다 —
  **1배 그대로 쓸 수 있어 격자도 도트도 완벽하다**

메인화면은 조우 연출과 같은 화면으로 만들 수 있다. 그림 + 아래 작은 실루엣 + 등불 +
`LightMenu`의 빛줄기 메뉴, 그리고 **"시작"을 누르면 그 안으로 걸어 들어간다.**
띄울 문장은 이미 정해져 있다 — *"세계를 건너다니는 자에게는 그곳이 온전히 보이지 않는다.
등불이 비추는 것만 실재한다."*

---

**PixelLab MCP를 이 컴퓨터에 붙였다**(2026-08-11). `https://api.pixellab.ai/mcp`, HTTP,
사용자 설정(`~/.claude.json`)에 user 스코프로 등록. **저장소 안 `.mcp.json`에는 넣지 않았다 —
`game_4`가 공개 저장소라 토큰이 그대로 올라간다.**

---

## 🚪 관문 그림을 뽑았고, 아웃페인팅에 60 gen을 태웠다 (2026-08-12)

메인화면 겸 출발점으로 **거대한 석조 관문**을 잡았다. 회원님이 주신 참고 그림은 붉은빛이 새어나오는
글자 새겨진 문틀(눈이 달린 것), 문이 겹겹이 이어지는 무한 복도 둘, 그리고 금색 고리 세계다.

### 뽑은 것 — 프롬프트와 시드를 짝으로 남긴다

| 파일 | 시드 | |
|---|---|---|
| `assets/photos/gate_1.png` | **미상** | 첫 시도. 골짜기에 처박혀서 실패 |
| `assets/photos/gate_2.png` | **미상** | 회원님이 "분위기 괜찮다"고 하신 것 |
| `assets/photos/gate_3.png` | **1000** | 가운데 정렬 시도. 검은 하늘은 나왔으나 밋밋해짐 |

`gate_3` 프롬프트 (512x512, 파라미터 없음):

```
a single tall narrow stone doorframe standing exactly in the middle of the picture, centred in
the frame, far taller than it is wide, standing alone on a flat empty plain, seen from low down,
worn glyphs over the stone, the bare flat ground running away to a distant horizon on both sides,
the opening looking through to dark empty distance, black sky, cold grey stone, red light only in
the opening
```

**gate_1·gate_2는 시드를 안 줘서 영영 재현 불가다.** MCP는 결과에 시드를 안 실어 보낸다 —
**호출할 때 직접 지정해야 남는다.** 웹 UI는 생성 후 시드를 알려주고 `Reuse seed` 버튼도 준다.

**`gate_4.png` (2026-08-12, 448x584, 시드 3000, 파라미터 없음)**

`gate_3`과 **똑같은 프롬프트**로 크기만 448x584로 바꿔 뽑았다. 세로를 512보다 72 높인 것은
pixen이 "면적 262,144 이내 · 변 768 이하 · 4의 배수"만 지키면 되므로 가로를 줄이면 세로를
높일 수 있기 때문이다(448x584 = 261,632).

밋밋하지 않게 나왔다 — 소용돌이치는 구름, 돌의 균열로 새는 붉은 빛, 포석, 왼쪽 멀리 지평선의
언덕. 세로가 길어진 만큼 문이 솟아 보인다.

### ★ pixen은 주제를 가운데에 안 놓는다 — 0.63이다

두 장을 실제로 재봤다(밝은 기둥 사이의 중점).

```
gate_2   문 중심 비율 0.632
gate_3   문 중심 비율 0.630   ← 프롬프트에 `centred in the frame`을 넣은 것
gate_4   문 중심 비율 0.62쯤  ← 크기도 시드도 다른데 또 같은 자리 (눈대중)
```

**세 장이 전부 같은 자리다. 우연이 아니라 pixen의 성질이다.**

**말로 시키는 게 아무 효과가 없다.** 시드도 프롬프트도 다른데 어긋난 양이 거의 같다.
그러니 **가운데 정렬에 generation을 쓰지 말고 놓는 자리로 잡는다** — `WalkScene`에 초점(`_focus_x`)을
넣어뒀고 기본값이 0.63이다. 그림·걷는 길·그것의 자리가 이 값 하나로 같이 움직인다.

### 아웃페인팅 실패 — 자세한 것은 `PIXELLAB.md` §10

캔버스를 644x512로 늘려 오른쪽을 이어 그리려다 **세 번 실패, 60 gen 소모.** 원인은
**`use selection tool to select painting area` 옵션을 안 켠 것**으로 보인다. 절차는 `PIXELLAB.md`
§2와 §7에 처음부터 적혀 있었다.

**실패해도 거부되지 않고 20 gen씩 정상 청구된다.** 결과를 반드시 눈으로 확인할 것.

### 자른 판(`gate_2c` · `gate_3c`)은 회원님이 안 쓰기로 하셨다

문을 가운데 맞추려고 왼쪽을 130px쯤 잘라낸 것인데, 잘라내는 방식 자체를 원치 않으셨다.
파일은 남아 있으니 필요 없으면 지우면 된다.

---

## 🚪 여는 화면이 완성됐다 (2026-08-12) — **generation 0**

**메인 씬이 `scenes/Opening.tscn`이다.** 켜면 이 화면이 나온다. 오늘 한 것은 전부 이미 있는
그림을 자르고 늘리고 흔든 것이라 **한 장도 새로 안 뽑았다.**

```
0.0~ 0.8s  캄캄하다
0.8~ 5.8s  여는 글이 한 단어씩 나온다 (11단어, 5초)
5.8~ 6.6s  다 쓰인 글을 그대로 둔다
6.6~ 7.6s  글이 사라진다
7.6~ 8.0s  다시 캄캄하다
8.0~ 8.8s  등불에만 불이 붙는다 — 세계는 아직 안 보인다
8.8~11.4s  그 불에서 빛이 번져 나가 화면을 덮는다
11.4~14.4s 그림이 옆으로 밀리며 관문이 드러난다
```

시간값은 전부 `Opening.gd` 맨 위 상수에 순서대로 모여 있다.

| 파일 | |
|---|---|
| `scenes/Opening.tscn` / `scripts/Opening.gd` | 연출 전부. 시간 하나로 화면을 정해서 아무 시점이나 캡처할 수 있다 |
| `shaders/PortalLeak.gdshader` | 관문에서 세계가 새어 나오는 왜곡. **새로 만든 것** |
| `tools/_cutout.gd` | 문 구멍을 투명하게 도려낸다 |
| `tools/_extend.gd` | 그림을 왼쪽으로 늘린다 |
| `tools/_silhouette.gd` | 등불 든 순례자 실루엣을 찍는다 |
| `assets/photos/archive.png` | 문 너머의 아카식 서고. 512x512, 시드 4000 |
| `assets/photos/pilgrim.png` | 실루엣. 44x80, 키 72px |
| `assets/photos/gate_4_wide.png` | 1260x584. 잘라내고 늘린 최종본 |

`-- --capture`를 붙여 실행하면 다섯 시점을 `tools/_opening_*.png`로 찍고 끝난다.

### ★ 자를 자리는 사람이 칠해주는 쪽이 훨씬 낫다

처음엔 "어두우면 문 안쪽"이라고 밝기로 골라 잘랐다. **하늘과 돌 그림자까지 먹어서** 잘린
범위가 x 201~357, y 22~574였다 — 문이 아니라 그림 절반이다.

회원님이 원본에서 **문 구멍을 순백으로 칠해주셨고**, 흰색만 고르게 바꾸니 한 번에 x 247~318,
y 132~505로 정확히 떨어졌다. 순백은 이 그림에 달리 없는 색이라 오검출이 없다.

**임계값을 붙들고 씨름하는 것보다 마스크를 손으로 칠하는 게 빠르다.**

### 늘리는 폭은 화면 폭 + 미는 거리다

448 + 512 = 960으로 딱 맞췄더니 **미는 동안 그림 왼쪽 끝이 검은 수직선으로 드러났다.**
밀 여유가 없으니 당연한 것이었다. 미는 거리 300을 더 붙여 1260으로 만들어 해결했다.

그리고 **문 구멍 위치를 손으로 옮겨 적던 것을 없앴다.** `_extend.gd`가 투명한 자리를 직접
재서 `Opening.OPENING`과 `SEAM`에 넣을 값을 찍어준다. 잘라내기를 다시 돌릴 때마다 조용히
어긋나던 숫자였다.

```
문 중심   0.631, 0.545   (늘리기 전)
문 중심   0.869, 0.545   ← Opening.OPENING
이음새    0.644          ← Opening.SEAM
```

### 일렁임은 둘로 갈렸다

회원님 주문이 "문 안쪽은 떨리고, 늘어난 쪽은 **거기서 흘러나왔으면** 좋겠다"였는데,
이 둘은 다른 물건이라 셰이더를 나눴다.

| | `PortalWave` (문 안쪽) | `PortalLeak` (늘어난 쪽) |
|---|---|---|
| 방향 | 없음 — 제자리 떨림 | 이음새에서 **바깥으로 진행** |
| 세기 | 균일 | 문 옆 0 → 왼쪽 끝 최대 |
| 늘어남 | 없음 | 세로로 최대 2.2배 |

**세로로 밀기만 해서는 "늘어난다"가 안 된다.** 굼실거릴 뿐이다. 읽는 범위를 좁혀 실제로
확대하는 것(`stretch`)을 같이 넣어야 치즈처럼 늘어진다.

### ★ "끝으로 갈수록 빨리 흐른다"를 속도로 만들면 안 된다

위치마다 속도를 다르게 주면 **시간이 갈수록 위상차가 무한정 벌어져서** 무늬가 점점 잘게
부서진다. 몇 초는 멀쩡해 보이지만 오래 켜두면 망가진다.

대신 **파장을 끝으로 갈수록 길게** 했다(`flow_gain`). 시간 주파수는 어디서나 같으니 파장이
길어진 만큼 마루가 그 배로 빨리 지나간다. 오래 켜둬도 그대로고, 흘러가며 늘어나는 물결이라
보기에도 맞는다. 파장이 거리에 비례해 늘어나므로 **지나온 파장 수가 로그**가 된다.

그리고 시작 지점이 티 나지 않게 두 가지를 했다.
- 나오는 자리를 이음새보다 **0.03 오른쪽**에 둬서 원본 그림 쪽으로도 살짝 넘어가게 했다
- 곧은 경사를 섞었다(`surge`). 끝에서만 몰아치게 두면 **늘어지기 시작하는 자리에 선이 보인다**

### 실루엣은 코드로 찍었다 — 두 번 만에 나왔다

통짜 검정이라 윤곽만 맞으면 되는 그림이다. 세로 위치별 반폭 표 하나로 그린다. 숫자만 고쳐
다시 돌리면 되고 **비용이 0이라 몇 번이고 바꿀 수 있다.**

- **팔을 몸에 붙이면 등불이 실루엣에 먹힌다.** 첫 판에서 옆구리의 혹으로만 보였다.
  관문 쪽으로 내미는 자세로 바꿔 살렸다
- 아랫단을 한 가지 주기로 흔들면 **빗살 같은 톱니**가 된다. 주기가 안 맞는 둘을 겹친다
- 목의 잘록함이 사람으로 읽히게 하는 유일한 단서다. 없으면 종 모양이 된다

**키 72px, 관문 470px.** 이제야 관문이 얼마나 큰지 읽힌다 — 실루엣이 없을 땐 그냥 큰 그림이었다.
그리고 허공에 해처럼 떠 있던 등불이 손으로 갔다(크기도 288 → 128).

등불은 색을 지키려고 필터 판 **위**에 있어서 세계와 같이 안 밀린다. 밀린 만큼을 매 프레임
더해서 손에 붙여 둔다.

### 여는 글을 넣었다

```
세계를 건너다니는 자에게는 그곳이 온전히 보이지 않는다.
등불이 비추는 것만 실재한다.
```

캄캄한 채로 **한 단어씩** 5초에 걸쳐 나오고, 잠깐 머물렀다가 사라진다. 그 다음에야 불이 켜진다.
**글을 먼저 읽히고 세계를 보여주는 순서다.**

**필터 판 위(layer 70)에 놓았다.** 세계에 속한 것이 아니라 세계를 두고 하는 말이라서이기도
하고, 아래에 두면 흐리게 뜨고 지는 동안 글자가 베이어 격자에 잘게 부서진다.

**둘째 줄은 반드시 한 줄로 둔다**(회원님). 이 문장이 게임의 규칙이라 쪼개면 힘이 빠진다.

### ★ 픽셀 글꼴은 크기를 마음대로 못 고른다

32px에서 첫 줄이 880px이라 960 화면에 40px씩만 남는다. "조금만 줄이자"는 주문이 나왔는데
**둥근모는 32 아니면 16뿐이었다** — 네이티브 16의 정수배가 아니면 도트 격자가 어긋난다.
글꼴별로 실제 폭을 재봤다.

```
둥근모   16px  440px      갈무리11  24px  644px
둥근모   32px  880px      갈무리9   30px  804px
```

24px로 줄이려면 글꼴을 바꿔야 해서, **회원님이 32px 유지를 택하셨다.** 앞으로도 글자 크기를
바꾸려면 이 표를 먼저 볼 것 — 중간 크기는 존재하지 않는다.

한 단어씩 드러내는 것은 `visible_characters`로 하는데, **Godot Label은 전체 문장 기준으로
배치를 잡고 뒤쪽 글자만 안 그린다.** 그래서 가운데 정렬이 단어마다 다시 계산되지 않고 이미
나온 단어가 옆으로 밀리지 않는다. 운이 좋았다.

### ★ 빛은 화면 가운데가 아니라 등불에서 나간다

처음엔 `light_position`을 화면 한가운데(0.5, 0.5)에 고정해두고 반경만 키웠다. 사방이 고르게
밝아져서 **빛의 출처가 없었다.** 등불을 순례자 손에 붙이고 나서 그 자리를 광원으로 넘기니
왼쪽 위가 어둡게 남아 등불이 비춘 화면이 됐다.

등불이 화면 오른쪽 아래에 치우쳐 있어서 반경이 1을 넘어야 반대편까지 닿는다(1.05).
**`hint_range`는 편집기 눈금일 뿐이고 코드로 넣는 값은 안 잘린다.**

### ★ 반경을 0으로 줘도 캄캄해지지 않는다

`DitherScreen`의 `lit`은 `1 - smoothstep(radius, radius + softness, 거리)`다. 반경이 0이어도
**번짐의 꼬리가 남아서 광원 자리에 흐린 원반이 계속 보인다.** 번짐도 같이 0으로 줄여야 진짜
어둠이 된다(`maxf(..., 0.001)` — 두 문턱값이 같으면 결과가 정의되지 않는다).

곧바로 불을 켜던 때는 안 보이던 문제다. **어둠 속에서 글을 읽히려고 하니 비로소 드러났다.**

### 다음에 할 것

1. **`scripts/walk/WalkScene.gd`의 `START_PHOTO`가 없는 파일(`gate_3.png`)을 가리킨다.**
   `Walk.tscn`을 열면 그대로 깨진다
2. `scripts/Portal.gd`는 만들어만 두고 **한 번도 안 돌렸다.** `Opening.gd`가 같은 일을 하고
   있어서 당장 필요가 없다 — 탐색 화면에서 관문을 쓸 때 검증한다
3. 여는 화면에서 **"시작"을 누르면 문 안으로 들어가는** 흐름
4. `tools/_opening_*.png`, `_zoom.png` 등 확인용 캡처가 `tools/`에 쌓여 있다

---

## 🧭 5단계로 나눠 만들기로 했다 (2026-08-13) — 회원님이 정하신 것

**여기부터가 게임을 실제로 만드는 계획이다.** 지금까지는 조각을 만들어 본 것이고, 이제
순서를 정해서 이어 붙인다.

| | 결과물 | 끝났다고 볼 기준 |
|---|---|---|
| 1 | 움직이는 주인공 | 지역을 걸어다닌다 |
| 2 | 조우 → 전투 | 무언가를 만나면 전투가 벌어지고 끝난다 |
| 3 | 첫 지역과 그 몬스터들 | 그 지역이 하나의 완결된 장소가 된다 |
| 4 | 레벨 · 능력 · 장비 | 싸운 결과가 캐릭터에 쌓인다 |
| 5 | 메인 씬 UI와 게임 내 UI | 눈에 보이는 것이 다 갖춰진다 |

### 지켜야 할 것 (회원님이 못박으신 것)

- **픽셀랩은 이 다섯 단계를 통틀어 200 generation을 넘지 않는다.** 잔액과는 별개인
  스스로 건 한도다
- **로직과 화면을 가른다.** 회원님 표현으로 "백엔드/프론트엔드". 이미 하고 있는 방식이다 -
  `Battle.gd`는 화면을 모르고 시그널만 쏘고 `BattleScreen.gd`가 받아 그린다.
  대사도 `DialogueController` / `DialogueUI`로 갈라져 있다
- **추가·제거가 쉬워야 한다.** 새 적은 리소스 파일 하나, 새 UI는 화면 쪽만 손대면 되게
- **단계마다 물어보고 진행한다.** 혼자 정하고 나아가지 않는다

### ★ 200을 세는 방법 — 잔액 차이로 세면 안 된다

**계정을 다른 곳에서도 쓰고 계신다**(회원님, 2026-08-13). 그래서 잔액이 줄어든 것이 곧 우리가
쓴 양이 아니다. 실제로 한 번 어긋났다 — 859에서 5를 썼는데 잔액은 842였다(17 감소).

**우리 몫은 호출할 때마다 응답에 찍히는 값을 더해서 센다.** 잔액은 "남았는지"만 보는 데 쓴다.

```
5단계에서 쓴 양   17 / 200   (2026-08-13 현재)

  캐릭터(등불 든 판) 1 + 걷기 8장 4 + 걷기 4장 4      = 9   ← 접었다
  캐릭터(빈손) 1 + 캐릭터(빈손 2) 1                    = 2   ← 접었다
  v3 회전 1 + 템플릿 걷기 4                            = 5   ← 쓰는 것
  그것(watcher) 1                                      = 1   ← 쓰는 것

  **11이 버려졌다.** 등불을 그림에 그려 넣은 것과, 시드 없이 굴려서 맞추려 한 것 탓이다.
  둘 다 위에 왜 그랬는지 적어뒀다.
```

### 200 generation 어림 (검증 안 된 값이다)

일지에 적힌 값에서 가져온 것이고, **실제로 부르기 전에 공식 문서로 확인하고 프롬프트·크기·
비용·잔액을 적어 보여준 뒤 멈춘다.**

```
1 주인공      10~20    8방향 + 걷기
2 조우·전투    0~10    hollow_armour와 사진들이 이미 있다
3 지역·몬스터  20~40    타일셋 2~3개 + 소품 + 몬스터 몇
4 레벨·장비     0~20    아이콘류. 코드로 그리면 0
5 UI           0~20    마찬가지
```

합쳐서 50~110쯤이라 200 안에 여유가 있다. **진짜 위험은 예산이 아니라 재작업이다** -
다시 뽑아도 같은 값이 나가고 실패한 것도 환불이 없다. 뽑기 전에 확정한다.

## 🧍 주인공 스프라이트 — 뽑은 설정 전부 (2026-08-13)

**다른 인물을 같은 화풍으로 뽑을 때 이 표를 그대로 쓴다.** `create_character`에는 시드가
없어서 똑같은 그림은 두 번 안 나온다. 화풍을 맞추는 유일한 수단이 **파라미터와 어휘를
그대로 쓰는 것**이다.

### 캐릭터 — `create_character`, 1 generation

```
a hooded pilgrim, heavy grey travelling cloak with a deep hood, face hidden
in shadow, carrying a small brass lantern in the right hand, worn leather
straps and satchel, bone-white trim, cold grey and dark brown, dark fantasy
```

| 항목 | 값 | 웹 UI에서 부르는 이름 |
|---|---|---|
| mode | `standard` | (기본) |
| size | **32** → 캔버스는 48x48로 나온다 | Canvas size |
| n_directions | **4** | Rotations |
| view | **`high top-down`** | View |
| outline | `selective outline` | Outline |
| shading | `basic shading` | Shading |
| detail | `medium detail` | Detail |
| body_type | `humanoid` | — |
| proportions | 기본 프리셋 | Proportions |
| text_guidance_scale | 8 (기본) | AI freedom(낮게) |

```
결과 id   6f88c0aa-dd74-4c1a-bade-5a863c83f249   (이름: 순례자)
```

**`standard`를 고른 이유는 4방향을 고를 수 있는 유일한 모드라서다.** `pro`(20~40 gen)와
`v3`(2~9 gen)는 **무조건 8방향**이다. 품질은 v3 > pro > standard 순이니, 더 좋게 뽑고 싶으면
v3로 8방향을 뽑고 4개만 쓰는 길이 있다.

**캔버스가 48로 나오는 것은 정상이다.** 애니메이션 여유로 40%쯤 커진다(32 → 48).
타일이 32px이라 인물은 딱 한 칸이다.

### 걷기 — `animate_character`, 방향당 1 generation

```
walking, carrying a lantern at the side
```

| 항목 | 값 |
|---|---|
| mode | **`v3`** |
| directions | south / north / east / west |
| frame_count | 8 → **9장**으로 나온다(기준 1 + 만든 8) |

**템플릿을 안 쓴다.** 공식 영상이 *"물건을 든 캐릭터에는 템플릿을 쓰지 마라 — 템플릿은
맨손 캐릭터로 학습돼서 검 든 기사에 쓰면 이상해진다"*고 못박는다. 등불을 든 우리 주인공이
정확히 여기 해당한다. v3는 비용이 같아서 손해도 없다.

**프레임 수는 이 크기에서 비용에 영향이 없다.** v3 값이 `가로x세로x프레임 ÷ 65536`의
올림이라, 48x48이면 16장을 뽑아도 방향당 1 gen이다. 장수는 비용이 아니라 보기로 정한다.

### 받은 뒤 하는 일 — 전부 0 generation

```
tools/_hood.gd     두건 속 얼굴을 검게 지운다 (살색 + 어둠에 갇힌 밝은 점)
tools/_frames.gd   방향별 프레임을 SpriteFrames로 엮는다 (idle_ / walk_ 로 갈라서)
```

`_frames.gd`의 `WALK_STEP`이 2면 받은 8장에서 한 장 걸러 **4장짜리 걸음**이 된다. 다시 뽑을
필요가 없다 — 걷기 고리는 접지·통과가 균등해서 짝수 번째만 빼도 위상이 맞는다.

### ★ 4장이 8장보다 나았다

처음에 8장으로 뽑았는데 **프레임마다 몸 폭이 흔들려서** 걸을 때 형체가 일렁였다. 4장으로
다시 뽑으니 형태가 안정적이고 다리 교차가 또렷하다. 32px짜리에 8장은 과했다 —
프레임 간 차이가 1픽셀도 안 되는 구간이 생기면 모델이 그 자리를 아무렇게나 채운다.

**한 장 걸러 4장으로 줄이는 것으로는 안 됐다.** 0 generation으로 장수는 줄었지만 자세 자체의
흔들림은 같은 그림이라 그대로 남았다. 결국 4 generation을 더 써서 새로 뽑았다.

```
walk/    8장짜리 (남겨둠)      walk4/   4장짜리 (지금 쓰는 것)
```

### 1단계에서 만든 것 — 로직과 화면을 갈랐다

| 파일 | 하는 일 |
|---|---|
| `scripts/map/Hero.gd` | 자리와 이동 규칙. **화면을 모른다.** 대각선을 여기서 거른다 |
| `scripts/map/HeroSprite.gd` | 방향과 걷는지 여부만 받아 그림을 고른다. **규칙을 모른다** |
| `scripts/map/Walker.gd` | 둘을 엮고 카메라·등불·필터를 맡는다 |
| `tools/_frames.gd` | 프레임을 `SpriteFrames`로 엮는다 |

**서 있는 그림에 generation을 안 썼다.** PixelLab이 걷기를 만들 때 원본 회전 그림을 첫 장으로
끼워 주는데 그게 곧 서 있는 자세다. `idle_` / `walk_`로 갈라두면 방향마다 idle을 따로 뽑을
필요가 없다(4 gen 절약).

### ★ 등불 빛이 캐릭터를 지운다

등불 빛은 **더하기 합성**이고 필터 판보다 위에 그려진다. 그런데 카메라가 나를 따라오니
나는 늘 화면 한가운데에 있고, 빛도 한가운데에 두면 **정통으로 겹쳐서 캐릭터가 통째로 씻긴다.**
조우 연출에서도 같은 것을 겪었다(그때는 실루엣을 빛 위에 그려서 풀었다).

여기서는 **빛을 손 위치로 옮기고 크기를 줄여서**(224 → 128) 풀었다. 방향마다 손 위치가
달라서 `Walker.LANTERN_AT`에 표로 들고 있다.

### ★ 손을 비우고 등불을 따로 얹기로 했다 (2026-08-13 오후)

위의 "등불 든 캐릭터"는 **접었다.** 걷기 프레임마다 등불의 위치와 모양이 튀었고, 물건을 든
캐릭터에는 템플릿 애니메이션도 못 써서 걷는 폼이 나빴다.

```
캐릭터   손을 비운다 → 템플릿 애니메이션이 먹는다 → 걷는 폼이 안정적이다
등불     코드로 찍어서(tools/_lantern.gd) HeroSprite가 따로 얹는다. 0 gen
```

그리고 **등불을 손에 들지 않고 앞에 띄운다**(회원님). *"비밀 결사니까 염력 정도는 쓰겠지."*
손에 매달면 팔 흔들림과 어긋나는데, 띄우면 그 문제가 통째로 사라진다 - 붙어 있을 이유가
없어지니까. 걸음과 **무관한 제 박자로** 위아래 흔든다(`FLOAT_HEIGHT` 2.5 / `FLOAT_SPEED` 1.6).

### ★★ 뽑기를 굴리지 말고 한 장을 정해놓고 돌려라 — v3 + 참조 그림

손을 비우려고 `create_character`를 두 번 더 굴렸는데 **세 번 다 딴사람이 나왔다.** 프롬프트를
거의 그대로 두고 한 구절만 바꿔도 톤이 갈렸다. 시드가 없으니 어휘로 붙잡을 수 있는 범위 밖이다.

답은 **`create_character` + `mode: v3` + `reference_image`** 였다. 도구 설명 그대로 옮기면
*"이 스프라이트를 그대로 여덟 방향으로 돌린다. 그림이 정체성과 화풍을 정하고, 설명은 회전만
안내한다."* 회원님이 손보신 정면 한 장을 넣으니 **같은 사람이 여덟 각도로** 나왔다.

```
create_character  mode=v3  reference_image_base64  size=32  view=high top-down
결과 78f73d44-...  16x32  8방향   비용 1 gen (문서의 2~9보다 쌌다)
```

- **참조는 캔버스를 잘라서 넣는다**(`tools/_reference.gd`). 48x48 캔버스에 담긴 32px 캐릭터를
  그대로 넣으면 결과가 어긋난다고 공식 자료에 적혀 있고, 실제로 여백 없이 16x32로 나왔다
- 비슷하지만 다른 것: **`pro` + `style_character_id`는 "다른 캐릭터, 같은 화풍"**이다.
  적·NPC를 주인공과 같은 화풍으로 만들 때 그쪽을 쓴다
- 걷기는 **템플릿 `walking-4-frames`**, 방향당 1 gen. 손이 비어서 쓸 수 있었고, 앞서 v3로
  뽑았을 때 "1번이 거의 서 있는 자세"였던 문제가 없다
- **템플릿은 4장이 전부 걷기다.** v3처럼 기준 프레임이 안 딸려온다 - 서 있는 그림은 회전
  원본을 따로 가져다 `_frames.gd`의 0번에 놓는다

### 🕯️ 조우를 선으로만 그리기로 했다 (2026-08-13)

`scenes/Encounter.tscn` / `scripts/walk/Encounter.gd`. **검은 화면에 흰 선뿐이다.**

앞선 방식(`Walk.tscn`, 일러스트 한 장을 화면에 채우기)에서 두 가지가 걸렸다. 그것이 배경과
같은 중간 회색이라 **묻혀서 안 보였고**(필터가 4계조로 깎으니 밝기를 올려도 같이 밝아진다),
장소마다 일러스트를 뽑아야 해서 값이 들었다.

선으로만 그리면 둘 다 사라진다. **묻힐 배경이 없고 뽑을 그림도 없다(0 gen).** 회원님이
언더테일 전투 화면을 예로 드셨는데, 그 형식이 우리 규칙(*"색을 가진 것은 등불뿐"*)과 같은
데서 나온 것이다 - 뺄 수 있는 걸 다 빼면 남는 게 그것이다.

- 화면 필터를 **안 씌운다.** 4계조 디더가 1픽셀 흰 선을 갉아먹고, 검정과 흰색뿐이라 할 일도 없다
- **선은 적을수록 낫다.** 16개는 빽빽했다(8개). 검은 자리가 이 화면의 알맹이다
- **그것은 처음부터 크다**(회원님). 점에서 자라나면 "나타나는" 것이 되는데, 그것은 나타나지
  않는다. 처음부터 거기 있었고 내가 다가갈 뿐이다
- 걷는 데 22초. 아무 일도 안 일어나는 시간이 외로움을 만든다
- 주인공은 탑다운 맵의 그것을 **북쪽(뒷모습) 걷기**로 그대로 쓴다

`Walk.tscn`은 안 지웠다. `Walker.ENCOUNTER_SCENE` 한 줄만 되돌리면 일러스트 방식으로 돌아간다.

### 🔭 그것 — 눈으로 뒤덮인 고리

`assets/enemies/watcher.png`. 회원님이 참고 그림을 주셨다(고리마다 눈이 박힌 구체, 성서에
나오는 천사의 모습). **아카식 서고 = 모든 것이 기록된 곳**을 지키는 게 모든 것을 보는 눈인
것은 말이 되고, `archive.png` 한가운데에 이미 고리로 둘러싸인 천구의가 있어서 **그게 깨어난
것처럼** 읽힌다.

```
create_image_pixen  512x512  no_background  lineless  highly detailed  side  seed 7000
비용 1 gen
```

- **pixen에는 시드가 있다.** 캐릭터와 달리 마음에 들면 재현된다. 반드시 지정해서 적어둘 것
- **`no_background`가 안 먹었다.** 켰는데도 배경이 불투명하게 딸려 왔다. `tools/_cutbg.gd`로
  도려냈다 - 색으로 고르면 그것의 어두운 부분까지 먹으니 **가장자리에서 이어진 곳만** 지우고,
  색으로 못 가르는 바깥 테는 **반지름으로** 잘랐다(원형이라 쓸 수 있는 수단이다)
- 원본은 `watcher_raw.png`에 남아 있다

### 🧵 서고 → 조우 → 전투가 한 줄로 이어졌다

`Walker`에 조우 자리를 넣었다(바닥의 가로 82% 지점). **자리를 표시해두지 않는다** - 어쩌다
마주치는 편이 사건이 된다. 조우 → 전투는 `WalkScene`이 이미 하고 있었다.

**깨져 있던 것을 고쳤다**: `WalkScene.START_PHOTO`가 저장소에 없는 `gate_3.png`를 가리키고
있어서 `Walk.tscn`을 열면 그대로 죽었다. 다른 컴퓨터에서 만들고 안 올린 파일로 보인다.

### 다음에 할 것

1. **조우 화면에 입체감이 없다.** 사각 테가 소실점을 중심으로 균등하게 벌어져서 복도가 아니라
   터널 정면으로 읽힌다. 소실점을 더 올리고 **바닥면만** 격자로 깔아야 서 있는 자리가 잡힌다
2. **서고다운 것을 넣는다**(회원님). 양옆으로 물러나는 책장 윤곽, 떠다니는 책·종이,
   바닥의 글자 격자. 선으로만 그리니 **0 gen**이다
3. 서고 맵 타일 — 지금은 바닥뿐이고 벽·책장이 없다. 어림 5 gen

### 겪은 것

- **같은 `action_description`으로 다시 부르면 중복으로 걸러진다.** 과금도 안 되고 아무 일도
  안 일어난다. 다시 뽑으려면 설명을 바꿔야 한다
- **애니메이션은 부를 때의 그림에서 만들어진다.** 웹에서 스프라이트를 고쳐도 이미 돌아간
  작업에는 반영이 안 된다. **고치고 나서 뽑을 것**
- 웹에서 손보면 상태가 하나 더 생길 수 있다(`Idle (copy)`). 원본이 거기 남아 있어서
  되돌릴 때 쓸모가 있었지만, 안 쓸 거면 지워야 나중에 내려받을 때 안 헷갈린다

---

PixelLab 잔액: **859 generations** (2000 중 1141 사용, 2026-08-12 확인).

**오늘 여는 화면 작업은 0 gen이다.** 다만 일지에 적힌 981에서 122가 줄어 있는데 그 사이
내역이 일지에 안 남아 있다 — **뽑을 때마다 여기에 적을 것.**
