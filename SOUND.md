# 소리 설명서

소리를 **코드로 찍는다**(`tools/_sfx.gd`). 이 문서는 그 방식에 대해 알아낸 것을 모은다.
`PIXELLAB.md`가 그림 쪽에 하는 일을 소리 쪽에 하는 것이다.

## 이 문서의 규칙

**출처에 우선순위가 있다.**

1. **공식 문서·표준 교재** — Godot 문서, DSP 교재. 수치와 한계
2. **우리 경험** — 맨 아래(§7). 위와 어긋나면 **위가 맞다**

`PIXELLAB.md`에서 배운 것이 있다. 짐작을 사실처럼 적어두면 다음 세션이 그걸 믿고 시간과 돈을
태운다. **확인 안 된 것은 "확인 안 됨"이라고 적는다.**

---

## 1. 우리가 이미 쓰는 것에는 이름이 있다

`_sfx.gd`는 교과서에 있는 기법들을 이름 없이 짜 놓은 상태다. **이름을 알면 더 찾을 수 있으므로**
먼저 붙여둔다.

| 우리 코드 | 이름 | |
|---|---|---|
| `_lowpass(state, input, k)` | **1극점 IIR 저역통과** | k가 곧 차단 주파수. 제일 싼 필터 |
| `band`/`low` 짝 + `f = 2·sin(π·fc/rate)` | **체임벌린 상태변수 필터(SVF)** | 저역·고역·대역을 한 번에 낸다 |
| 그 안의 `0.9` `0.55` `0.34` | **감쇠 계수 = 1/Q** | 작을수록 좁고 날카롭다 |
| `_room()`의 네 번 겹치기 | **멀티탭 딜레이 / 초기 반사** | 진짜 잔향의 앞부분만 흉내낸 것 |
| `hz * 1.503` 같은 어긋난 배음 | **모달 합성(modal synthesis)** | 물체를 때렸을 때 우는 모드들 |
| 430Hz + 437Hz | **맥놀이(beating)** | 두 음의 차이가 곧 울렁이는 주기 |
| 잡음 × `exp(-t*k)` | **잡음 버스트** | 타격음의 기본 재료 |
| `randf_range(-1,1)` | **백색 잡음** | 모든 주파수가 고르게 들어 있다 |

---

## 2. 왜 사인파는 금속으로 들리는가

`_sfx.gd`에 세 번 적혀 있는 발견이다 — 34Hz 사인은 대포, 205Hz 사인은 핀셋, 1900Hz 사인은 종.

**이유가 있다. 순수한 사인파에는 재료가 없다.**

현실의 물체는 때리면 **여러 모드**로 동시에 운다. 그 모드들의 주파수 비율이 그 물체가 무엇으로
만들어졌는지를 말해준다.

```
정수배 (2.00, 3.00, 4.00)     종·악기.  곱게 울린다
어긋난 비 (1.50, 3.57, 7.12)  쇠붙이·판. 깽 하고 운다
아주 조밀하고 불규칙          돌·나무.  거의 잡음에 가깝다
```

사인 하나만 쓰면 모드가 **하나뿐인 물체**가 되는데, 현실에 그런 물체가 없다. 귀는 그걸
"악기 아니면 금속"으로 분류한다.

**그래서 두 갈래다.**

- **재료가 있는 소리**(발소리·돌·흙) → **잡음을 걸러서** 만든다. 우리가 이미 하는 방식
- **울리는 물건**(쇠·유리·종) → **모드를 몇 개 겹쳐서** 만든다. `_tick`이 이걸 우연히 했다

`_tick`의 주석에 *"정수배면 종이 되고 어긋나야 쇠붙이가 된다"*고 적혀 있는데, 그게 정확히
모달 합성이다. **모드 서너 개와 각각의 감쇠만 있으면 웬만한 타격음이 된다** — 그리고 재료마다
알려진 비율표가 있으니 짐작으로 1.503을 넣는 것보다 낫다.

출처: [Modal synthesis](https://en.wikipedia.org/wiki/Modal_synthesis) ·
[Cook, Physically-Based Parametric Sound Synthesis](https://www.cs.princeton.edu/~prc/CookSig00.pdf)

---

## 3. Q값이 "신시사이저 ↔ 파도" 축이다

`_wind`에 적힌 발견이다 — *"좁게 거르면 신시사이저, 넓게 거르면 파도. 바람이 그 사이에 있다."*

이건 **필터의 Q**를 말로 표현한 것이다. 우리 SVF에서 `high = noise - low - D*band`의 **D가 1/Q**다.

```
D 작다 (0.1 이하)   Q 높다   좁다   음높이가 또렷 → 삐- 하는 시험음
D 0.3~0.6           중간             결은 있는데 음은 안 선다 → 바람·숨
D 1.0 이상          Q 낮다   넓다   그냥 색만 입힌 잡음 → 파도·소음
```

우리 값: `_wind` 0.34(바람), `_shard` 0.55(빛살), `_step` 0.9(발소리). **소재가 단단할수록
넓게 열었다** — 우연이 아니라 맞는 방향이다.

**중심 주파수를 시간에 따라 움직이는 것이 "무엇이 지나갔다"를 만든다.** `_shard`가 3200→700Hz로
훑어 내리고 `_wind`가 560Hz 언저리를 오르내리는 것이 그거다.

---

## 4. 잡음에도 색이 있다

우리는 `randf_range(-1,1)`(백색)만 쓰고 저역통과로 어둡게 만든다. 그런데 자연의 소리는
대개 백색이 아니다.

| | 스펙트럼 | 어디에 |
|---|---|---|
| **백색** | 고름 | 타격의 순간, 지직거림 |
| **분홍** | 옥타브당 -3dB | **바람·비·먼 소음.** 자연에서 제일 흔하다 |
| **갈색** | 옥타브당 -6dB | 우르릉, 저역 울림 |

`_lowpass`를 한 번 통과시킨 백색 잡음은 갈색에 가깝다. **분홍 잡음이 바람에는 더 맞는다** —
저역통과 하나로는 안 나오고, 만드는 방법에 이름이 있다 (2026-08-18 확인).

- **Kellet 방식** — 차단점이 다른 1차 필터 서넛의 **가중합.** 9.2Hz 위에서 ±0.05dB까지
  맞는 계수가 공개돼 있다. 우리 `_lowpass`를 서넛 겹치면 바로 된다
- **Voss-McCartney 방식** — 갱신 주기가 제각각인 백색 잡음 여럿을 더한다. 한 표본마다
  하나만 갱신하면 되는 꼴로 다듬어져 있어 아주 싸다

`_wind`가 "겨울바람이 파도가 됐다"고 여러 번 고생한 기록이 있는데, **잡음 색을 바꾸는 것도
그 축의 손잡이 하나**다. (우리 코드로는 아직 안 해봤다)

출처: [DSP Generation of Pink Noise](https://www.firstpr.com.au/dsp/pink-noise/)

---

## 5. 아직 안 써본 기법

### Karplus-Strong

짧은 잡음을 **필터가 걸린 지연선에 되먹임**해서 튕긴 줄이나 때린 물체를 만든다. 코드가
열 줄쯤이고 아주 싸다.

```
버퍼를 잡음으로 채운다 → 한 칸씩 꺼내 쓰면서, 꺼낸 값과 다음 값의 평균을 뒤에 다시 넣는다
버퍼 길이가 음높이, 평균 낼 때의 가중치가 감쇠 속도
```

**쇠·나무·줄에 잘 맞는다.** `_tick`(돌이 갈리는 소리)이나 칼 소리를 다시 만들 때 후보다.

출처: [CCRMA — The Karplus-Strong Algorithm](https://ccrma.stanford.edu/~jos/pasp/Karplus_Strong_Algorithm.html) ·
[Karplus–Strong string synthesis](https://en.wikipedia.org/wiki/Karplus%E2%80%93Strong_string_synthesis)

### 제대로 된 잔향

`_room()`은 네 번 겹치는 **초기 반사**뿐이다. 진짜 잔향은 그 뒤에 오는 **빽빽한 꼬리**가
있는데, 슈뢰더 방식(빗살 필터 넷 + 올패스 둘)이 그걸 싸게 만든다.

**다만 우리는 이걸 직접 짤 필요가 없을 수도 있다 — §6 참고.**


### FM 합성

한 사인의 **주파수를 다른 사인으로 흔든다.** 음색이 크게 변하면서도 음이 잘 안 서서,
**무조(無調) 드론**에 흔히 쓰이는 방식이다. 물방울 떨어지는 신비한 소리 같은 것도 여기서 나온다.

`_drone`은 지금 사인 몇 개를 **더하기만**(가산) 해서 얌전한 편이다. FM으로 바꾸면 훨씬
기괴해질 여지가 있고, 코드는 몇 줄 차이다.

### 그래뉼러 합성

소리를 **몇 밀리초짜리 알갱이로 잘게 썰어** 순서를 바꾸고, 늘이고, 겹치고, 음을 바꿔 다시
붙인다. 원본이 무엇이었는지 알 수 없게 되면서 질감만 남는다. **서고의 웅성거림**이나
"뭔가 말하는 것 같은데 안 들리는" 소리에 맞는다.

### 유리가 깨지는 소리 (등불이 꺼질 때 쓸 것)

지금 등불이 꺼지는 소리는 빛살 소리를 낮춰 쓴 "팟"인데, 등불은 놋쇠 통에 **유리**가 끼워진
물건이다. **쩌적쩌적쩌적 쩅그랑**이 맞다.

```
금 가는 소리   아주 짧은 파열음 여럿을 불규칙한 간격으로 흩는다 (그래뉼러가 여기 맞다)
깨지는 소리    여러 높이의 공진을 한꺼번에 울리고 제각각 다른 속도로 재운다
```

**높은 모드가 먼저 죽어야** 쇳조각이 아니라 유리로 들린다 — `_tick`에서 이미 쓴 요령이다.

### 발소리를 인자로 받기

찾아보니 발소리는 한 덩어리가 아니라 여러 토막이다 — **어택**(발을 끌며 다가오는 앞소리),
**텀프**(닿는 순간), **테일**(떼면서 남는 꼬리). 여기에 **바닥 재질**(돌·나무·낙엽)과
**걷는 이의 무게·속도**가 각각 다른 값으로 붙는다.

`_step()`이 이미 셋으로 나뉘어 있으므로, **재질과 속도를 인자로 빼면** 서고 바닥과 다른 데가
갈린다. 프로시저럴 오디오가 원래 잘하는 일이 이것이다(실행 중에 값을 받아 소리를 만드는 것).

참고로 발소리는 합성이 어려운 축이라 **신경망으로 생성하는 연구**까지 있다 —
[GAN으로 발소리 만들기](https://arxiv.org/pdf/2110.09605). 우리만 헤맨 게 아니다.
---

## 6. ★ 울림을 파일에 굽지 말고 버스에 걸 것 (확인 필요)

지금 `_save()`가 **모든 소리에 `_room()`을 구워 넣는다.** 그래서 두 가지가 걸린다.

1. **도는 소리(바람·공포)는 빼야 한다** — 꼬리가 이음매를 망치므로. 코드에 예외가 생겼다
2. **나중에 못 바꾼다** — 서고와 협곡과 사막의 울림이 다를 텐데, 구워버리면 장소마다 다시 뽑아야 한다

Godot에는 **오디오 버스**에 거는 `AudioEffectReverb`가 있고, **실행 중에 바꿀 수 있다.**
동굴에 들어가면 잔향을 켜고 나오면 끄는 식이다.

```
파일에 굽기    확정적이고 CPU를 안 쓴다. 대신 도는 소리에 못 쓰고 나중에 못 바꾼다
버스에 걸기    도는 소리에도 걸린다. 장소마다 바꿀 수 있다. CPU를 조금 쓴다
```

**이 게임은 "꿈마다 다른 세계"가 전제라 버스 쪽이 맞아 보인다.** 소리는 마른 채로 뽑아두고
울림은 장소가 정하는 구조다.

다만 **아직 안 해봤다.** Godot 내장 잔향의 소리가 우리 멀티탭보다 나은지, 픽셀아트 톤에
어울리는지는 귀로 확인해야 한다.

출처: [Audio buses](https://docs.godotengine.org/en/stable/tutorials/audio/audio_buses.html) ·
[Audio effects](https://docs.godotengine.org/en/stable/tutorials/audio/audio_effects.html) ·
[AudioEffectReverb](https://docs.godotengine.org/en/stable/classes/class_audioeffectreverb.html)

---

## 7. 우리 경험

**여기 있는 것은 확인된 사실이 아니라 우리가 겪은 일이다.** 위 절과 충돌하면 위가 맞다.

### 세 가지 (2026-08-14)

1. **사인파를 쓰면 무조건 금속으로 들린다.** 34Hz 사인 → 대포, 205Hz 사인 → 핀셋,
   1900Hz → 종. 재료가 있는 소리는 **잡음을 걸러서** 만든다 → §2에 이유가 있다
2. **좁게 거르면 신시사이저, 넓게 거르면 파도. 바람이 그 사이에 있다** → §3이 그 축이다
3. **"안 들린다"를 파형 탓으로 넘기지 말 것.** 울음이 안 들린다고 했는데 재보니 열여섯 중
   제일 큰 파일이었다. 진짜 원인은 **같은 순간에 겹친 다른 소리**였다

3번이 제일 값어치 있다. **소리는 귀로 판단하기 전에 재야 한다**(`tools/_probe_sfx.gd`).
그리고 안 들리는 원인은 대개 그 소리가 아니라 **가리는 쪽**에 있다 — 심리음향에서 마스킹이라
부르는 것이고, 특히 **낮은 소리가 높은 소리를 잘 가린다.**

### 그 밖에 겪은 것

- **낮다고 무거운 게 아니다.** 울음을 104→71Hz로 잡았더니 작은 스피커에서 안 나왔다.
  세기를 올리는 대신 **음을 올려서**(233→165Hz) 해결했다
- **고르게 흐르면 바람, 끊기면 갈리는 소리.** `_tick`이 잡음을 31Hz로 잘라 "그그그긍"을 만든다
- **음이 아래로 미끄러져야 가라앉는 것이 된다.** `_seize`·`_hurt`·`_over`가 다 그렇게 한다
- **도는 소리는 길이에 딱 떨어지는 주기만 써야** 이음매가 안 들린다. 잡음은 이음매가 안
  들리므로 **세기와 밝기만** 주기에 맞추면 된다(`_wind`)

### 저장 (다시 겪지 말 것)

- **`.tres`로 저장한다.** `.wav`로 저장하면 `ResourceSaver`가 그 확장자를 못 다뤄서
  **아무것도 안 쓰고 조용히 실패한다.** 저장 실패를 반드시 오류로 띄울 것
- 진짜 `.wav` 파일이 필요하면(**밖에서 들어보거나 Audacity로 살펴볼 때**) 전용 함수가 따로
  있다 — [`AudioStreamWAV.save_to_wav(path)`](https://docs.godotengine.org/en/stable/classes/class_audiostreamwav.html).
  2026-08-18에 알았다. `ResourceSaver`와는 다른 물건이다
- mp3는 임포트에 **루프가 꺼진 채** 들어온다. 불러온 자리에서 켜 준다(`Music.play_in()`)
- 다 **모노 22050Hz 16비트.** 저역과 잡음이 대부분이라 더 필요 없고 저장소에도 가볍다

---

## 8. 더 읽을 것

- **Andy Farnell, 『Designing Sound』(MIT Press)** — 이 분야의 교과서다. *"어떤 소리든
  맨바닥에서 만들 수 있다"*는 전제로 물리와 청각부터 시작해 Pure Data로 하나씩 짓는다.
  **발소리·바람·불·비·물·총·차량 모델이 통째로 들어 있다** — 우리가 필요한 것과 거의 겹친다.
  Pd 예제지만 우리가 GDScript로 옮기는 데 무리가 없다(둘 다 표본 단위 처리다).
  [출판사 소개](https://aspress.co.uk/ds/about_book.html) ·
  [저자 인터뷰](https://designingsound.org/2012/01/18/procedural-audio-interview-with-andy-farnell/)
- **sfxr / Bfxr / jsfxr** — 8비트 효과음 생성기. 파라미터 몇 개로 뽑는 구조라, **어떤 손잡이가
  어떤 느낌을 만드는지** 보기에 좋다. 우리 방식과 결이 같다
- [Godot 오디오 문서](https://docs.godotengine.org/en/stable/tutorials/audio/audio_buses.html) — 버스·효과·실행 중 변경
- **freesound.org** — CC 라이선스 무료 음원. **발소리와 바람은 여기서 받아 쓰는 쪽이 낫다.**
  현실에 있는 소리는 폴리(직접 녹음)를 이길 방법이 마땅치 않다. 반대로 울음·공포 드론·빛살처럼
  **현실에 없는 소리는 합성이 낫다** — 라이브러리에도 마땅한 게 없고, 숫자만 바꿔 다시 뽑을 수 있다
- **Sonniss GDC 번들** — 매년 무료로 푸는 프로 라이브러리. 상업 이용 가능
- **Audacity(무료)의 컨볼루션 잔향** — 실제 공간에서 딴 임펄스 응답(IR)을 씌우면 그 공간
  소리가 그대로 난다. §6의 버스 잔향과 함께 저울질할 것
